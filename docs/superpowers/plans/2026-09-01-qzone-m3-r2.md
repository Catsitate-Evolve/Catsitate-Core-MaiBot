# QQ空间 M3 修正（第二轮）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按《QQ空间模块完整设计稿（三期终稿）》落实 13 项裁定：日程属性分离、通知独立于窗口、通知带时间、发布保留 tid、发现层翻页、表达生成层（三工具两段式）、view_friend_feeds、发布主动触发+冷启动自举、见闻系统、点赞源C、日记人设前置、场景/文档收尾。

**Architecture:** 全部改动限于插件目录（`MaiBot-dev/plugins/catsitate_core_maibot/`，独立 git 仓库）。信息流（注入/泵/存储）与动作流（工具/表达生成/写 API）按设计稿 §2-§4 演进；所有正文产出走「planner 传 reply_reference → 旁路人设 LLM 生成」两段式。

**Tech Stack:** Python 3.11+ / sqlite3 / maibot-plugin-sdk（@Tool、@HookHandler、capability：llm.generate、database.get、config.get、maisaka.proactive.trigger）/ pytest（离线，stub 为主）。

**Spec:** `docs/superpowers/specs/2026-09-01-qzone-full-design.md`（本计划从该规格出发，执行者须同时读规格）

## Global Constraints

- 只改插件目录，不碰 MaiBot 主程序（`MaiBot-dev/` 其余部分只读）。
- 用户可见文本/日志/注释/文档一律简体中文；文档自包含（不引用开发历史、不出现「联调缺陷#N/用户裁定/AR-N」类内部指代）。
- 错误显式暴露：无静默兜底；回退必须告警；解析失败不猜测。
- 只用 SDK 声明 API；新能力须在 `_manifest.json` 声明（本批仅用已声明能力，预计无增补）。
- 每任务 TDD：先写失败测试→实现→过测→全量套件→commit（在插件仓库内）。
- 测试离线：不触网，客户端用 stub，LLM 用 stub。
- 测试命令统一：`cd /home/hesitate-p/Catsitate/MaiBot-dev/plugins/catsitate_core_maibot && python -m pytest tests/ -q`（单测时替换为目标节点）。

---

### Task 1: 日程属性分离 read_qzone / send_qzone

**Files:**
- Modify: `catsitate_core/schedule.py:85-86`（validate）、`:117-119`（fix）、`:171-172`（overview 文案）
- Modify: `catsitate_core/llm_provider.py:54-68`（SIDE_TEMPLATES["schedule_generate"]）
- Modify: `prompt_templates/catsitate_schedule_generate.prompt`
- Modify: `plugin.py:1010`（窗口判定）
- Test: `tests/test_schedule.py`

**Interfaces:**
- Produces: 窗口 dict 新键 `read_qzone: bool`、`send_qzone: bool`（仅 daily 窗口合法；旧键 `qzone` 不再消费）。后续 Task 8/9 依赖这两个键名。

- [ ] **Step 1: 写失败测试**

在 `tests/test_schedule.py` 追加（沿用该文件现有的 validate_schedule/fix_schedule 导入方式）：

```python
def test_validate_schedule_attribute_split():
    """read_qzone/send_qzone 仅 daily 窗口合法;qzone 旧键不再消费。"""
    base = {"date": "2026-09-02", "windows": [
        {"kind": "sleep", "start": "2026-09-01T23:00", "end": "2026-09-02T07:30"},
        {"kind": "daily", "start": "2026-09-02T09:00", "end": "2026-09-02T12:00",
         "activity": "窝着刷手机", "read_qzone": True, "send_qzone": True},
    ]}
    data, err = validate_schedule(base)
    assert err == "" and data is not None
    # 非daily窗口带任一新键 → 拒绝
    bad = {"date": "2026-09-02", "windows": [
        {"kind": "sleep", "start": "2026-09-01T23:00", "end": "2026-09-02T07:30", "read_qzone": True},
        {"kind": "daily", "start": "2026-09-02T09:00", "end": "2026-09-02T12:00", "activity": "x"},
    ]}
    data2, err2 = validate_schedule(bad)
    assert data2 is None and "read_qzone" in err2
    bad2 = {"date": "2026-09-02", "windows": [
        {"kind": "sleep", "start": "2026-09-01T23:00", "end": "2026-09-02T07:30"},
        {"kind": "greeting", "start": "2026-09-02T09:00", "end": "2026-09-02T10:00",
         "activity": "早安", "send_qzone": True},
    ]}
    data3, err3 = validate_schedule(bad2)
    assert data3 is None and "send_qzone" in err3


def test_fix_schedule_strips_attributes_on_nondaily():
    day = "2026-09-02"
    data = {"date": day, "windows": [
        {"kind": "sleep", "start": "2026-09-01T23:00", "end": "2026-09-02T07:30",
         "read_qzone": True, "send_qzone": True},
        {"kind": "daily", "start": "2026-09-02T09:00", "end": "2026-09-02T12:00",
         "activity": "窝着刷手机", "read_qzone": True},
    ]}
    fixed = fix_schedule(data, day)
    sleep_win = fixed["windows"][0]
    assert "read_qzone" not in sleep_win and "send_qzone" not in sleep_win
    assert fixed["windows"][1].get("read_qzone") is True
```

（若 test_schedule.py 尚未导入 fix_schedule，补 import；断言旧 `qzone` 键被 fix 剥除的行为若现有测试存在则同步改。）

- [ ] **Step 2: 跑测确认失败**

Run: `python -m pytest tests/test_schedule.py -q`
Expected: 新增用例 FAIL（validate 仍按旧 `qzone` 键校验，`read_qzone` 放行/误报）。

- [ ] **Step 3: 实现**

`schedule.py:85-86` 替换为：

```python
        for attr in ("read_qzone", "send_qzone"):
            if w.get(attr) and w.get("kind") != "daily":
                return None, f"{attr} 属性仅允许 daily 窗口: {w}"
```

`schedule.py:117-119`（fix_schedule 非 daily 剥除）替换为：

```python
        if w.get("kind") != "daily":
            for attr in ("qzone", "read_qzone", "send_qzone"):
                w.pop(attr, None)
```

`schedule.py:171-172`（schedule_overview_text 标注）替换为：

```python
            if w.get("read_qzone"):
                activity += "(刷空间)"
            if w.get("send_qzone"):
                activity += "(发说说)"
```

（原行为是 `qzone` 键标 `(刷空间)`；读语义归 read_qzone，发语义新增。）

`plugin.py:1010` 替换为：

```python
            in_qzone_window = bool(win and win.get("kind") == "daily" and win.get("read_qzone"))
```

`llm_provider.py` SIDE_TEMPLATES["schedule_generate"]：`"version": 3` → `4`，窗口结构行与语义说明替换为：

```python
            '- 活动窗口(1~8 个):{"kind": "greeting" 或 "daily", "start": "...", "end": "...", "activity": "活动描述", "plan_speak": true/false, "topic": "发言主题(计划发言时)", "read_qzone": true/false, "send_qzone": true/false(两者均仅 daily 可标)}\n'
            ...
            "read_qzone=true 标记该窗口会刷QQ空间(浏览好友动态):仅 kind=daily 窗口可标,通常一天 1~2 个,适合搭配轻松的独处活动(如「窝着刷手机」)。\n"
            "send_qzone=true 标记该窗口会有感而发写说说(可与 read_qzone 同窗:刷完想分享;也可独立:忙里偷闲发一条):仅 kind=daily 窗口可标,通常一天 0~2 个。\n"
```

同时把该模板里「活动描述贴合猫耳少女人设(发呆/看雨/散步/听音乐等)」改为「活动描述贴合素材中给定的人设(独处/休闲类活动为主)」——人设以素材传入，模板不写死。`prompt_templates/catsitize_schedule_generate.prompt` 按同一文案同步改写（保持与内置默认一致；prompt_deploy 会自动同步部署）。

- [ ] **Step 4: 跑测通过**

Run: `python -m pytest tests/test_schedule.py tests/test_llm_provider.py tests/test_integration.py -q`
Expected: PASS（若集成测试断言旧 `qzone` 键/文案，按新语义更新断言）。

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(schedule): 窗口属性分离 read_qzone/send_qzone"
```

---

### Task 2: 通知独立于浏览窗口（P1/P2 分治）

**Files:**
- Modify: `catsitate_core/qzone/injector.py:52-57`（window_ended）、`:91-104`（next_to_inject）
- Modify: `plugin.py:1191`（泵门控）、`:1011-1021`（窗口结束块）
- Test: `tests/test_qzone_injector.py`

**Interfaces:**
- Produces: `FeedInjector.next_to_inject(now)` 新语义——P1（通知）任何时刻可弹（仅 awaiting 互斥）；P2（浏览）仅 `window_active` 时可弹。`window_ended()` 只清 P2/awaiting，**保留 P1**。`stats()` 键不变。

- [ ] **Step 1: 写失败测试**

`tests/test_qzone_injector.py` 追加（沿用现有构造 `FeedInjector(decision_window_s=...)` 与 FeedItem 造法）：

```python
def test_p1_injectable_without_window():
    """通知是推送语义:窗口未开也能注入。"""
    inj = FeedInjector(decision_window_s=10)
    inj.enqueue_priority([_feed("n1", "100")])
    assert inj.next_to_inject(0.0) is not None


def test_p2_blocked_without_window():
    """浏览动态只在窗口内注入。"""
    inj = FeedInjector(decision_window_s=10)
    inj.enqueue([_feed("f1", "100")])
    assert inj.next_to_inject(0.0) is None
    inj.window_started()
    assert inj.next_to_inject(0.0) is not None


def test_window_ended_keeps_p1():
    """窗口结束清浏览队列,通知队列保留等待注入条件。"""
    inj = FeedInjector(decision_window_s=10)
    inj.window_started()
    inj.enqueue([_feed("f1", "100")])
    inj.enqueue_priority([_feed("n1", "200")])
    inj.window_ended()
    st = inj.stats()
    assert st["p2_queued"] == 0 and st["p1_queued"] == 1
    assert inj.next_to_inject(0.0) is not None  # P1 仍可注入
```

（`_feed` 为该文件已有的造 FeedItem 辅助；若签名不同按现状适配。）

- [ ] **Step 2: 跑测确认失败**

Run: `python -m pytest tests/test_qzone_injector.py -q`
Expected: 新用例 FAIL（窗口未开 next_to_inject 恒 None / window_ended 清空 P1）。

- [ ] **Step 3: 实现**

`injector.py:52-57` 替换为：

```python
    def window_ended(self) -> None:
        """浏览窗口结束:清浏览队列与进行中的注入。

        通知队列(P1)保留——通知是推送语义,不隶属任何窗口,
        等注入条件(bot 醒着/泵空闲)满足后继续。
        """
        self._window_active = False
        self._p2.clear()
        self._awaiting = None
        self._popped = None
```

`injector.py:91-104`（next_to_inject）窗口检查替换为：

```python
        if self._awaiting is not None:
            return None
        if self._p1:
            return self._p1.pop(0)  # 通知:推送语义,不依赖浏览窗口
        if self._window_active and self._p2:
            return self._p2.pop(0)  # 浏览动态:仅 read_qzone 窗口内注入
        return None
```

`plugin.py:1191` 泵门控：

```python
            if not self._qzone_available:
                return
```

（删除 `or not self.qzone_injector.window_active`；:1193 睡眠检查保留。）

`plugin.py:1011-1021` 窗口结束块替换为：

```python
            if not in_qzone_window:
                if self.qzone_injector.window_active:
                    self.qzone_injector.window_ended()
                    reverted = self.qzone_seen.revert_pending()
                    self.ctx.logger.info(
                        "QQ空间浏览窗口结束,浏览队列回退未读(%d 条);通知队列保留等待注入", reverted
                    )
                return
```

（原 `p1_left` 告警分支删除——P1 不再被清空。）

- [ ] **Step 4: 跑测通过**

Run: `python -m pytest tests/test_qzone_injector.py tests/test_qzone_wiring.py tests/test_integration.py -q`
Expected: PASS（若现有用例断言「窗口外泵不动/窗口结束清 P1」，按新语义更新）。

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(qzone): 通知注入独立于浏览窗口(P1 推送语义)"
```

---

### Task 3: 通知参数行带动作时间

**Files:**
- Modify: `catsitate_core/qzone/messages.py`（新增 `format_comment_param_line`）
- Modify: `plugin.py` 源A/源B 通知正文构造（`_qzone_notify_scan` 内两处 `〔说说ID=` 拼装）
- Test: `tests/test_qzone_gateway.py`

**Interfaces:**
- Produces: `format_comment_param_line(*, feed_tid: str, comment_tid: str = "", commenter_uin: str = "", action: str = "评论", create_time: str = "", now_epoch: float = 0.0) -> str`——输出如 `〔说说ID=ee3396c49d38 评论ID=2 评论者QQ=10001 评论于今天14:30〕`；时间缺失时省略时间段。

- [ ] **Step 1: 写失败测试**

`tests/test_qzone_gateway.py` 追加：

```python
from catsitate_core.qzone.messages import format_comment_param_line


def test_format_comment_param_line_with_time():
    now = datetime.now().timestamp()
    create = str(int(now - 60))
    line = format_comment_param_line(
        feed_tid="ee3396c49d38abcdef", comment_tid="2", commenter_uin="10001",
        action="评论", create_time=create, now_epoch=now,
    )
    assert line.startswith("〔说说ID=ee3396c49d38 评论ID=2 评论者QQ=10001 评论于")
    assert line.endswith("〕")


def test_format_comment_param_line_without_time():
    line = format_comment_param_line(
        feed_tid="ee3396c49d38abcdef", comment_tid="2", commenter_uin="10001",
        action="回复", create_time="", now_epoch=0.0,
    )
    assert line == "〔说说ID=ee3396c49d38 评论ID=2 评论者QQ=10001〕"
```

（该文件已有 `from datetime import datetime` 则复用。）

- [ ] **Step 2: 跑测确认失败**

Run: `python -m pytest tests/test_qzone_gateway.py -q`
Expected: FAIL（ImportError: format_comment_param_line 不存在）。

- [ ] **Step 3: 实现**

`messages.py` 在 `comment_time_prefix` 之后新增：

```python
def format_comment_param_line(
    *, feed_tid: str, comment_tid: str = "", commenter_uin: str = "",
    action: str = "评论", create_time: str = "", now_epoch: float = 0.0,
) -> str:
    """通知参数行:ID 锚 + 动作时间(评论于/回复于 M月d日 HH:MM)。

    动作时间让 bot 分得清互动新旧(新鲜度窗口内 3 天前的评论与 3 分钟前的
    长得一样);create_time 缺失/非法时省略时间段,不编造时间。
    """
    parts = [f"说说ID={str(feed_tid)[:12]}"]
    if str(comment_tid or "").strip():
        parts.append(f"评论ID={comment_tid}")
    if str(commenter_uin or "").strip():
        parts.append(f"评论者QQ={commenter_uin}")
    tag = comment_time_prefix(create_time, now_epoch)
    if tag:
        parts.append(f"{action}于{tag}")
    return "〔" + " ".join(parts) + "〕"
```

`plugin.py` `_qzone_notify_scan` 源A content 构造（:1392-1395 一带）替换为：

```python
                        content=(
                            f"评论了你的说说:{parse_qzone_mentions(c.content, bot_uin=bot_uin)}\n"
                            + format_comment_param_line(
                                feed_tid=feed_tid, comment_tid=c.comment_tid, commenter_uin=str(c.uin),
                                action="评论", create_time=str(c.create_time or ""), now_epoch=now_epoch,
                            )
                        ),
```

源B 通知正文里同款 `〔说说ID=… 评论ID=… 评论者QQ=…〕` 拼装处（源B 段内第二处 `〔说说ID=`）同样替换为 `format_comment_param_line(..., action="回复", create_time=<该回复的 create_time>, now_epoch=now_epoch)`。plugin.py 顶部从 `catsitate_core.qzone.messages` 的 import 增补 `format_comment_param_line`。

- [ ] **Step 4: 跑测通过**

Run: `python -m pytest tests/test_qzone_gateway.py tests/test_qzone_wiring.py -q`
Expected: PASS（现有通知格式断言若不含时间则更新为匹配新参数行）。

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(qzone): 通知参数行携带动作时间(评论于/回复于)"
```

---

### Task 4: do_publish 保留新说说 tid（回注锚 + registry + seen）

**Files:**
- Modify: `catsitate_core/qzone/wire.py`（新增 `extract_publish_tid`）
- Modify: `catsitate_core/qzone/client.py:356-364`（do_publish 返回 str）
- Modify: `plugin.py` qzone_post 回注块（:855-880）、`_generate_and_publish_diary`（:2439-2448）、`_echo_pending_diary`（:2450-2483）
- Test: `tests/test_qzone_wire.py`

**Interfaces:**
- Produces: `extract_publish_tid(payload: dict) -> str`；`client.do_publish(*, content: str) -> str`（新 tid，取不到为空串——发布已成功，仅影响回注锚，调用方告警）。qzone_post/日记回注文本带 `〔说说ID=xxx〕`；发布成功即 `qzone_seen.mark_queued + mark_seen`（summary=正文前 50 字）与 `qzone_registry.register(FeedContext(tid, owner_uin=bot_uin, owner_nickname="我", kind="self"))`。Task 6/10 依赖 seen 里的 own-post 摘要与 registry 自锚。

- [ ] **Step 1: 写失败测试**

`tests/test_qzone_wire.py` 追加：

```python
from catsitate_core.qzone.wire import extract_publish_tid


def test_extract_publish_tid_variants():
    assert extract_publish_tid({"data": {"tid": "abc123"}}) == "abc123"
    assert extract_publish_tid({"tid": "def456"}) == "def456"
    assert extract_publish_tid({"content": [{"tid": "ghi789"}]}) == "ghi789"
    assert extract_publish_tid({"code": 0, "data": {}}) == ""
```

- [ ] **Step 2: 跑测确认失败**

Run: `python -m pytest tests/test_qzone_wire.py -q`
Expected: FAIL（ImportError）。

- [ ] **Step 3: 实现**

`wire.py` 新增：

```python
def extract_publish_tid(payload: dict) -> str:
    """从发布响应载荷提取新说说 tid。

    键形态按端点历史版本逐层尝试(顶层 tid / data.tid / content[0].tid);
    取不到返回空串由调用方告警——发布本身已成功,tid 缺失只影响回注锚,
    不应误报发布失败。
    """
    data = payload.get("data") if isinstance(payload.get("data"), dict) else {}
    for candidate in (payload.get("tid"), data.get("tid"), data.get("newtid"), payload.get("newtid")):
        value = str(candidate or "").strip()
        if value:
            return value
    content = payload.get("content") if isinstance(payload.get("content"), list) else []
    if content and isinstance(content[0], dict):
        value = str(content[0].get("tid") or "").strip()
        if value:
            return value
    return ""
```

`client.py:356-364` do_publish 末尾 `return True` 改为：

```python
        payload = await self._post(self.PUBLISH_URL, form=form, referer_uin=self.bot_uin,
                                   extra_params={"uin": self.bot_uin})
        return extract_publish_tid(payload)
```

（docstring 返回值说明同步改为「新说说 tid,取不到为空串」。）

`plugin.py` qzone_post：`await self.qzone_client.do_publish(content=content)` 改为 `tid = await ...`；`if not tid: self.ctx.logger.warning("QQ空间说说发布成功但响应未含新说说 tid,回注缺锚")`；回注 raw_message 文本改为：

```python
            "raw_message": [{"type": "text",
                             "data": f"我发布了一条说说:{content[:60]}" + (f"\n〔说说ID={tid[:12]}〕" if tid else "")}],
```

route 成功后新增（echo 的 try 块之后、成功日志之前）：

```python
        if tid:
            try:
                self.qzone_seen.mark_queued(tid, abstime=str(int(time.time())), author_uin=bot_uin,
                                            summary=content[:50], author_nickname="我")
                self.qzone_seen.mark_seen(tid, datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                                          echo_msg["message_id"])
                self._qzone_registry.register(FeedContext(
                    tid=tid, owner_uin=bot_uin, owner_nickname="我", kind="self",
                    content_summary=content[:100],
                ))
            except Exception:
                self.ctx.logger.exception("QQ空间说说发布后本地锚定失败(远端已成功,仅告警)")
```

`_generate_and_publish_diary`：`tid = await self.qzone_client.do_publish(content=diary_text)`；快照 save 增键：`{"text": diary_text, "published_at": iso, "tid": tid}`。`_echo_pending_diary`：echo 文本 `f"我昨晚发布的日记:{text[:60]}"` 后接 `(f"\n〔说说ID={data.get('tid','')[:12]}〕" if data.get('tid') else "")`；route 成功清快照前，tid 非空时同款 `mark_queued/mark_seen/register`（summary=text[:50]）。

- [ ] **Step 4: 跑测通过**

Run: `python -m pytest tests/test_qzone_wire.py tests/test_qzone_client.py tests/test_qzone_wiring.py -q`
Expected: PASS（do_publish 相关既有断言由 `True` 改为 tid 字符串）。

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(qzone): 发布保留新说说 tid(回注锚/registry/seen)"
```

---

### Task 5: 发现层翻页 + 拉取数量配置

**Files:**
- Modify: `catsitate_core/config.py:236-237` 后新增 3 字段
- Modify: `catsitate_core/qzone/client.py:242-282`（_fetch_unified/get_unified_timeline 增 begin/scope）
- Modify: `plugin.py` 发现层调用（:1034-1044）、源A num（:1349）、源B discovery（:1428）
- Test: `tests/test_qzone_client.py`、`tests/test_qzone_wiring.py`

**Interfaces:**
- Produces: 配置 `discovery_count:int=50`、`discovery_max_pages:int=3`、`own_feed_scan_count:int=20`；`get_unified_timeline(*, count=20, begin=0)`；`get_like_events`（Task 10）复用 `_fetch_unified(*, count, scope)`。

- [ ] **Step 1: 写失败测试**

`tests/test_qzone_client.py` 追加（沿用该文件 stub fetch 的造法）：

```python
async def test_fetch_unified_begin_scope_passthrough():
    """begin/scope 透传到请求参数(翻页与与我相关流共用通道)。"""
    captured = {}

    async def fetch(method, url, *, params, headers, timeout_ms, data=None):
        captured.update(params)
        return 200, b'{"code":0,"data":{}}'

    client = _make_client(fetch)  # 该文件已有的构造辅助
    await client._fetch_unified(count=50, begin=50, scope=1)
    assert captured["begin"] == "50" and captured["count"] == "50" and captured["scope"] == "1"
```

`tests/test_qzone_wiring.py` 追加翻页循环用例（stub `qzone_client.get_unified_timeline` 记录 (count, begin) 调用序列）：

```python
async def test_discovery_pagination_stops_on_all_seen():
    """本页无新 tid 即止步:稳态 1 次调用;两页后全旧则停在第 2 页。"""
    p, ctx = _make_plugin_with_qzone(tmp_path)  # 该文件已有的插件+stub 构造
    calls = []

    async def page(*, count, begin=0):
        calls.append(begin)
        if begin == 0:
            return [_disc("t1", "100", "300"), _disc("t2", "101", "200")]  # t1/t2 已 seen
        return [_disc("t3", "102", "100")]

    p.qzone_client.get_unified_timeline = page
    p.qzone_seen.mark_queued("t1", abstime="300", author_uin="100", summary="a")
    p.qzone_seen.mark_queued("t2", abstime="200", author_uin="101", summary="b")
    await p._qzone_poll_feeds()
    assert calls == [0]  # 第 1 页全部已见,不再翻页
```

（`_disc` 为 FeedDiscovery 造法；`_make_plugin_with_qzone` 若无则在文件内新建：构造插件实例、stub ctx、置 `_qzone_available=True`、`_schedule_data` 含 read_qzone daily 当前窗口、`qzone_injector.window_started()` 预置。）

- [ ] **Step 2: 跑测确认失败**

Run: `python -m pytest tests/test_qzone_client.py tests/test_qzone_wiring.py -q`
Expected: FAIL（begin/scope 不存在；单次调用无翻页）。

- [ ] **Step 3: 实现**

`config.py` 在 `summary_days`（:237）后新增：

```python
    discovery_count: int = _f(50, "发现层单页拉取条数(统一时间线)", label="发现页大小")
    discovery_max_pages: int = _f(3, "发现层翻页上限(长时间离线后的积压补全;稳态恒 1 次调用)", label="发现翻页上限")
    own_feed_scan_count: int = _f(20, "通知源A扫描自己最近 N 条说说的评论(单次请求条数,不增加调用次数)", label="自扫说说数")
```

`client.py` `_fetch_unified(self, *, count: int)` → `(self, *, count: int, begin: int = 0, scope: int = 0)`；params 中 `"begin": str(begin)`、`"scope": str(scope)`。`get_unified_timeline(self, *, count: int = 20, begin: int = 0)` 透传 begin（scope 恒 0）。新增：

```python
    async def _fetch_likes_raw(self, *, count: int) -> str:
        """拉取「与我相关」流原始文本(feeds3_html_more?scope=1,源C 赞事件输入)。"""
        return await self._fetch_unified(count=count, scope=1)
```

（Task 10 的 get_like_events 消费它。）

`plugin.py` 发现层（:1034-1044）替换为翻页循环：

```python
            page_size = max(self.config.qzone.discovery_count, 1)
            max_pages = max(self.config.qzone.discovery_max_pages, 1)
            discoveries: list[FeedDiscovery] = []
            try:
                for page_idx in range(max_pages):
                    batch = await self.qzone_client.get_unified_timeline(
                        count=page_size, begin=page_idx * page_size
                    )
                    discoveries.extend(batch)
                    has_new = any(
                        d.appid == FEED_APPID_SHUOSHUO and self.qzone_seen.is_new_candidate(d.tid)
                        for d in batch
                    )
                    if not batch or not has_new:
                        break  # 本页无新说说:更早页只会更旧,翻页止步
            except QzoneAuthError:
                self.qzone_cookie.invalidate()
                self.ctx.logger.warning("QQ空间登录态失效(统一时间线),cookie 已作废,下轮重取")
                return
            except Exception:
                self.ctx.logger.exception("QQ空间统一时间线拉取失败,回退逐好友旧路径")
```

（后续过滤/充实代码不动——`discoveries` 变量名与既有下游一致；`FEED_APPID_SHUOSHUO` 若未导入则从 `catsitate_core.qzone.protocol` 补 import。）

源A：`get_own_feed_comments(bot_uin=bot_uin, num=10)` → `num=max(self.config.qzone.own_feed_scan_count, 1)`。源B：`get_unified_timeline(count=20)` → `count=max(self.config.qzone.discovery_count, 1)`（单页不翻页——源B 只需找「有新活动且评论过」的交集）。

- [ ] **Step 4: 跑测通过**

Run: `python -m pytest tests/test_qzone_client.py tests/test_qzone_wiring.py tests/test_config.py -q`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(qzone): 发现层翻页与拉取数量配置(消除积压静默丢失)"
```

---

### Task 6: 表达生成层（qzone_comment/qzone_reply/qzone_post 两段式）

**Files:**
- Create: `catsitate_core/qzone/expression.py`
- Create: `prompt_templates/catsitate_qzone_expression.prompt`
- Modify: `catsitate_core/llm_provider.py`（SIDE_TEMPLATES 增 qzone_expression）
- Modify: `catsitate_core/config.py`（expression_llm_model/expression_llm_timeout_ms + tool_whitelist 描述）
- Modify: `catsitate_core/qzone/protocol.py:20-50`（FeedItem 增 comments）
- Modify: `catsitate_core/qzone/client.py:228-232`（get_user_feeds 合并评论摘要）
- Modify: `catsitate_core/qzone/registry.py:19-28`（FeedContext 增 content_summary/recent_comments）
- Modify: `plugin.py`：三个工具签名与正文（:707-880）、泵登记块（:1260-1274）
- Test: `tests/test_qzone_expression.py`（新建）、`tests/test_qzone_registry.py`、`tests/test_qzone_client.py`

**Interfaces:**
- Consumes: `build_side_prompt(template_id, stable_ctx, variable_tail)`（llm_provider.py:174）、`_persona_context()`（plugin.py:3152）、`_side_llm_call(messages, model, module, timeout_ms)`（plugin.py:2848）。
- Produces: `generate_action_text(llm_call, *, mode: str, persona: str, reference: str, style: str = "", context_lines: list[str] | None = None, limit: int = 200, logger=None) -> tuple[str, str]`（(正文, 错误串)，二选一）；`FeedContext(content_summary: str = "", recent_comments: list[str] = [])`；`FeedItem(comments: list[str] = [])`。工具参数：`reply_reference`(必填)/`reply_style`(简短表达/正常回复/长回复)。

- [ ] **Step 1: 写失败测试**

新建 `tests/test_qzone_expression.py`：

```python
import pytest

from catsitate_core.qzone.expression import generate_action_text


def _llm(responses):
    calls = []

    async def call(messages):
        calls.append(messages)
        if isinstance(responses, list):
            item = responses.pop(0) if responses else {"success": False}
        else:
            item = responses
        return item

    return call, calls


@pytest.mark.asyncio
async def test_generate_success():
    call, calls = _llm({"success": True, "response": " 哈哈这个好可爱 "})
    text, err = await generate_action_text(call, mode="comment", persona="猫耳少女",
                                           reference="夸一下她的猫", limit=200)
    assert err == "" and text == "哈哈这个好可爱"
    assert calls[0][0]["role"] == "system"
    assert "猫耳少女" in calls[0][1]["content"]  # 人设前置为稳定上下文首段


@pytest.mark.asyncio
async def test_overlong_regenerates_then_truncates():
    call, _ = _llm([
        {"success": True, "response": "长" * 300},
        {"success": True, "response": "短正文"},
    ])
    text, err = await generate_action_text(call, mode="post", persona="p", reference="r", limit=200)
    assert err == "" and text == "短正文"
    call2, _ = _llm([
        {"success": True, "response": "长" * 300},
        {"success": True, "response": "还是长" * 100},
    ])
    text2, err2 = await generate_action_text(call2, mode="post", persona="p", reference="r", limit=200)
    assert err2 == "" and len(text2) == 200  # 重生成仍超长→截断


@pytest.mark.asyncio
async def test_failure_and_empty_reference_and_bad_style():
    call, _ = _llm({"success": False})
    text, err = await generate_action_text(call, mode="comment", persona="p", reference="x")
    assert text == "" and "失败" in err
    # 空 reference 显式报错,不猜
    call_blank, _ = _llm({"success": True, "response": "ok"})
    _, err2 = await generate_action_text(call_blank, mode="comment", persona="p", reference="  ")
    assert err2 != ""
    # 空响应报错
    call_empty, _ = _llm({"success": True, "response": ""})
    _, err3 = await generate_action_text(call_empty, mode="reply", persona="p", reference="x")
    assert "空" in err3
    # 非法 style 归一为「正常回复」(prompt 素材段可断言),未知 mode 报错
    call_style, calls = _llm({"success": True, "response": "ok"})
    _, err4 = await generate_action_text(call_style, mode="comment", persona="p",
                                         reference="r", style="瞎写")
    assert err4 == "" and "正常回复" in calls[0][1]["content"]
    _, err5 = await generate_action_text(call_style, mode="unknown", persona="p", reference="r")
    assert "未知动作类型" in err5
```

（最后一个用例断言非法 style 不抛异常即可——按 `calls[0][2]` 含「正常回复」实现；写实现时把它改成清晰的 await 版本。）

`tests/test_qzone_registry.py` 追加：

```python
def test_feed_context_new_fields_default():
    ctx = FeedContext(tid="t", owner_uin="1")
    assert ctx.content_summary == "" and ctx.recent_comments == []
```

`tests/test_qzone_client.py` 追加：

```python
async def test_get_user_feeds_merges_comments():
    """FeedItem.comments 取该说说 commentlist 前 3 条「昵称:内容(40字)」。"""
    payload = _msglist_payload_with_comments()  # 按该文件已有造法,commentlist 一条
    client = _make_client_with_payload(payload)
    feeds = await client.get_user_feeds(target_uin="100", nickname="小明", num=3)
    assert feeds and feeds[0].comments and feeds[0].comments[0].startswith("小红:")
```

- [ ] **Step 2: 跑测确认失败**

Run: `python -m pytest tests/test_qzone_expression.py tests/test_qzone_registry.py tests/test_qzone_client.py -q`
Expected: FAIL（模块/字段不存在）。

- [ ] **Step 3: 实现**

新建 `catsitate_core/qzone/expression.py`：

```python
"""表达生成层:空间动作正文由带完整人设的旁路 LLM 产出。

两段式分工(与主程序 reply 工具/planner 同构):planner 决定「是否动作、
表达什么方向」(reply_reference/reply_style),本层按人设写正文——planner
提示词只含行为风格摘要,完整人设在表达层,空间动作与真实聊天由此共享
同一个人设出口。
"""

from __future__ import annotations

from typing import Any, Awaitable, Callable

from catsitate_core.llm_provider import build_side_prompt

MODE_LABELS = {"comment": "评论好友的说说", "reply": "回复好友的评论", "post": "发一条自己的说说"}
VALID_STYLES = ("简短表达", "正常回复", "长回复")


def _sanitize(text: str) -> str:
    """输出卫生:剥首尾引号与空白(模型偶尔给正文套引号)。"""
    return text.strip().strip('"“”').strip()


async def generate_action_text(
    llm_call: Callable[[list[dict]], Awaitable[dict[str, Any]]],
    *, mode: str, persona: str, reference: str, style: str = "",
    context_lines: list[str] | None = None, limit: int = 200, logger: Any = None,
) -> tuple[str, str]:
    """生成动作正文。返回 (正文, 错误串),二者互斥;失败不静默兜底。"""

    if mode not in MODE_LABELS:
        return "", f"未知动作类型:{mode}"
    reference = (reference or "").strip()
    if not reference:
        return "", "缺少 reply_reference(表达方向)"
    style = style if style in VALID_STYLES else "正常回复"
    context_lines = [str(x) for x in (context_lines or []) if str(x).strip()]
    stable_ctx = [
        f"bot 人设:{(persona or '').strip() or '(未配置,按轻松自然的口吻)'}",
        f"动作:{MODE_LABELS[mode]};篇幅:{style};正文上限 {limit} 字",
    ]
    variable_tail = ["表达方向(planner 的意图说明,不是正文,不要照抄):\n" + reference] + context_lines
    messages, _ = build_side_prompt("qzone_expression", stable_ctx, variable_tail)
    result = await llm_call(messages)
    if not isinstance(result, dict) or not result.get("success"):
        return "", "表达生成 LLM 调用失败"
    text = _sanitize(str(result.get("response") or ""))
    if not text:
        return "", "表达生成返回空文本"
    if len(text) > limit:
        if logger:
            logger.warning("QQ空间表达生成超长(%d 字>上限 %d),带字数硬约束重新生成一次", len(text), limit)
        retry_messages, _ = build_side_prompt(
            "qzone_expression", stable_ctx,
            variable_tail + [f"硬约束:正文不超过 {limit} 字,超出将被截断。"],
        )
        result2 = await llm_call(retry_messages)
        if isinstance(result2, dict) and result2.get("success"):
            retry_text = _sanitize(str(result2.get("response") or ""))
            if retry_text:
                text = retry_text
    if len(text) > limit:
        if logger:
            logger.warning("QQ空间表达生成重生成仍超长(%d 字),截断至 %d 字", len(text), limit)
        text = text[:limit]
    return text, ""
```

`llm_provider.py` SIDE_TEMPLATES 增（列表尾）：

```python
    # 空间动作表达生成(评论/回复/发布共用,mode/reference 在素材段)
    "qzone_expression": {
        "version": 1,
        "system": (
            "你就是这位用户本人(人设见素材首段),要在QQ空间执行一个动作,按「表达方向」写出正文。\n"
            "要求:\n"
            "- 以人设的身份、口吻和习惯用词书写,像平时说话,不是转述\n"
            "- 「表达方向」是意图说明,不要照抄,要写出成形的自然正文\n"
            "- 不加引号/表情符号/@/前后缀,直接输出正文\n"
            "- 用简体中文"
        ),
    },
```

新建 `prompt_templates/catsitate_qzone_expression.prompt`（内容=同一段 system 文本，纯文本无引号包裹）。

`config.py` QzoneSection 增（diary 字段后）：

```python
    expression_llm_model: str = _f("memory", "表达生成模型(评论/回复/说说正文的旁路人设生成)", label="表达生成模型")
    expression_llm_timeout_ms: int = _f(0, "表达生成超时(毫秒,0=默认)", label="表达生成超时(ms)")
```

`protocol.py` FeedItem 增字段：`comments: list[str] = field(default_factory=list)`。

`client.py` get_user_feeds：

```python
    async def get_user_feeds(self, *, target_uin: str, nickname: str, num: int = 5) -> list:
        payload = await self._fetch_msglist(target_uin=target_uin, num=num)
        feeds = parse_msglist(payload, target_uin=str(target_uin), nickname=nickname)
        comments = parse_feed_comments(payload)
        for f in feeds:
            f.comments = [f"{c.nickname}:{c.content[:40]}" for c in comments.get(f.tid, [])[:3]]
        return feeds
```

（`parse_feed_comments` 从 wire 补 import。）

`registry.py` FeedContext 增：`content_summary: str = ""`、`recent_comments: list[str] = field(default_factory=list)`（该文件已有 dataclass/field 导入则复用）。

`plugin.py` 泵登记块（:1260-1274）FeedContext 构造增两参：

```python
                content_summary=((feed.origin_content if feed.source == "notify" else feed.content) or "(无文字)")[:100],
                recent_comments=list(feed.comments),
```

三个工具改造（以 qzone_comment 为例，:707-769 整体替换；qzone_reply/qzone_post 同理）：

```python
    @Tool(
        "qzone_comment",
        description="评论好友的QQ空间说说。reply_reference 填你想表达的方向(要点/事实/语气),正文会按你的口吻生成。",
        brief_description="评论说说",
        parameters=[
            ToolParameterInfo(name="feed_id", param_type="string", description="目标说说ID(照抄消息尾部「说说ID=」)", required=True),
            ToolParameterInfo(name="reply_reference", param_type="string", description="想表达什么:要点/事实/关系/语气倾向(不是正文,正文按人设生成)", required=True),
            ToolParameterInfo(name="reply_style", param_type="string", description="篇幅:简短表达/正常回复/长回复(默认正常回复)", required=False),
            ToolParameterInfo(name="at_user_id", param_type="string", description="回应评论时填评论者QQ,会自动@TA", required=False),
        ],
        visibility="visible",
    )
    async def qzone_comment(self, feed_id: str = "", reply_reference: str = "", reply_style: str = "",
                            at_user_id: str = "", **kwargs: Any) -> str:
        if not self._qzone_available:
            return "QQ空间模块未启用。"
        stream_id = str(kwargs.get("stream_id") or "")
        if stream_id not in self._qzone_session_id_set():
            return "这个工具只能在QQ空间动态流里使用。"
        if not feed_id.strip():
            return "缺少说说ID,请照抄消息末尾〔〕里的说说ID。"
        fid, owner_uin, ctx = self._qzone_resolve_feed(feed_id)
        if not fid:
            return f"未找到说说 {feed_id[:12]},可能已过期,请核对消息尾部的说说ID。"
        count = self._qzone_comment_counts.get(fid, 0)
        if count >= 3:
            return "这条说说你已经评论过 3 次了,适可而止～"
        # 表达生成层:场景素材(说说正文+作者+近期评论防复读)
        ctx_lines = [f"说说作者:{(ctx.owner_nickname if ctx else '') or owner_uin}"]
        if ctx and ctx.content_summary:
            ctx_lines.append(f"说说内容:{ctx.content_summary}")
        if ctx and ctx.recent_comments:
            ctx_lines.append("该说说下的近期评论:" + ";".join(ctx.recent_comments))
        persona, _ = await self._persona_context()
        content, err = await generate_action_text(
            partial(self._side_llm_call, model=self.config.qzone.expression_llm_model,
                    module="qzone_expression", timeout_ms=self.config.qzone.expression_llm_timeout_ms),
            mode="comment", persona=persona, reference=reply_reference, style=reply_style,
            context_lines=ctx_lines, limit=200, logger=self.ctx.logger,
        )
        if err:
            self.ctx.logger.warning("QQ空间评论正文生成失败:%s", err)
            return f"评论正文生成失败({err}),可以稍后重试。"
        # ……以下 @ 前缀/do_comment/记账 与原实现一致(content 即生成正文)
```

（`from functools import partial` 与 `from catsitate_core.qzone.expression import generate_action_text` 补 import；@ 前缀拼装、频控记账沿用原代码。）

qzone_reply：参数改 `feed_id* / comment_id* / reply_reference* / reply_style?`，正文生成 `mode="reply", limit=200, context_lines=[说说内容 summary]`，其余（二元组/@/do_reply/记账）沿用。qzone_post：参数改 `reply_reference* / reply_style?`，`mode="post", limit=500, context_lines=[]`，其余（tid/回注/锚定）沿用 Task 4 成果。三个工具 description 同步改写（planner 可见，自包含）。

- [ ] **Step 4: 跑测通过**

Run: `python -m pytest tests/test_qzone_expression.py tests/test_qzone_registry.py tests/test_qzone_client.py tests/test_qzone_wiring.py -q`
Expected: PASS（wiring 中旧 content 参数用例改 reply_reference + stub llm）。

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(qzone): 表达生成层——三动作工具两段式(reply_reference→人设正文)"
```

---

### Task 7: view_friend_feeds 工具 + inspect_image 的 image_hash 路径

**Files:**
- Modify: `plugin.py`（qzone_post 后新增 view_friend_feeds 工具；inspect_image :576-634 增 image_hash 参数与分支）
- Modify: `catsitate_core/config.py:224-230`（白名单默认 + view_friend_feeds）
- Test: `tests/test_qzone_wiring.py`（或新建 `tests/test_qzone_view.py`）

**Interfaces:**
- Consumes: `client.get_user_feeds`、`client.download_image`、`fit_images_to_rpc_budget`（messages.py）、`FeedContext`。
- Produces: 工具 `view_friend_feeds(qq*, count?) -> dict | str`——dict 含 `content`(文本摘要,逐图列 sha256 前 8 位)与 `content_items`(content_type=image+data(base64)+mime_type)；成功即 registry 登记。`inspect_image(..., image_hash?)`：image_hash 给定时跳过消息搜索，按前缀查 Images 表。

- [ ] **Step 1: 写失败测试**

`tests/test_qzone_wiring.py` 追加：

```python
async def test_view_friend_feeds_returns_media_dict():
    p, ctx = _make_plugin_with_qzone(tmp_path)
    feed = _feeditem("tid1", uin="100", nickname="小明", content="今天天气好",
                     image_urls=["http://img.qpic.cn/a.jpg"])
    async def get_user_feeds(*, target_uin, nickname, num=3):
        return [feed]
    async def download(url):
        return b"fakejpeg"
    p.qzone_client.get_user_feeds = get_user_feeds
    p.qzone_client.download_image = download
    result = await p.view_friend_feeds(qq="100", count=2)
    assert isinstance(result, dict)
    assert "作者:小明" in result["content"] and "说说ID=tid1" in result["content"]
    assert "图1(" in result["content"]
    assert result["content_items"] and result["content_items"][0]["content_type"] == "image"
    resolved = p._qzone_registry.resolve("tid1")
    assert resolved and resolved.owner_uin == "100" and resolved.content_summary.startswith("今天天气好")


async def test_inspect_image_by_hash_prefix(tmp_path, monkeypatch):
    p, ctx = _make_plugin_with_qzone(tmp_path)
    async def cap(capability, **kwargs):
        if capability == "database.get":
            return {"image_hash": "abcd1234" + "0" * 56, "full_path": "data/img/x.jpg"}
        return {}
    ctx.call_capability = cap
    (tmp_path / "x.jpg").write_bytes(b"img")
    monkeypatch.setattr("plugin_mod.Path", lambda s: tmp_path)  # 按该文件既有 stub 方式适配 /MaiMBot 根
    result = await p.inspect_image(image_hash="abcd1234", question="图里是什么")
    assert "取图失败" not in str(result) or True  # 主断言:走 hash 路径未因消息搜索失败
```

（第二个用例主断言按实现细化为「_fetch_recent 未被调用」——用调用计数 stub 断言。）

- [ ] **Step 2: 跑测确认失败**

Run: `python -m pytest tests/test_qzone_wiring.py -q`
Expected: FAIL（view_friend_feeds 不存在；inspect_image 无 image_hash 参数）。

- [ ] **Step 3: 实现**

`plugin.py` 在 qzone_post 之后新增：

```python
    @Tool(
        "view_friend_feeds",
        description="查看指定QQ好友最近的QQ空间说说(正文+图片)。想了解某位好友近况时使用,任何聊天里都可用。",
        brief_description="看好友说说",
        parameters=[
            ToolParameterInfo(name="qq", param_type="string", description="好友QQ号", required=True),
            ToolParameterInfo(name="count", param_type="integer", description="看最近几条(默认3,上限10)", required=False),
        ],
        visibility="visible",
    )
    async def view_friend_feeds(self, qq: str = "", count: int = 3, **kwargs: Any) -> dict | str:
        """全域查看工具:真实流仅供获取信息(空间动作工具在真实流隐藏)。"""
        import base64
        import hashlib

        if not self._qzone_available:
            return "QQ空间模块未启用。"
        qq = str(qq).strip()
        if not qq.isdigit():
            return "请提供好友的QQ号(纯数字)。"
        count = max(1, min(int(count or 3), 10))
        try:
            feeds = await self.qzone_client.get_user_feeds(target_uin=qq, nickname=qq, num=count)
        except QzoneAuthError:
            self.qzone_cookie.invalidate()
            self.ctx.logger.warning("QQ空间登录态失效(view_friend_feeds),cookie 已作废,下轮重取")
            return "登录态失效已重置,请稍后再试。"
        except Exception:
            self.ctx.logger.exception("QQ空间好友说说拉取失败(qq=%s)", qq)
            return "拉取失败,已记录日志。"
        if not feeds:
            return f"{qq} 最近没有可见的说说。"
        text_parts: list[str] = []
        content_items: list[dict] = []
        for f in feeds:
            self._qzone_registry.register(FeedContext(
                tid=f.tid, owner_uin=qq, owner_nickname=f.nickname or qq, kind="feed",
                content_summary=(f.content or "(纯图)")[:100], recent_comments=list(f.comments),
            ))
            pairs: list[tuple[str, bytes]] = []
            for url in f.image_urls[:3]:
                data = await self.qzone_client.download_image(url)
                if data:
                    pairs.append((url, data))
            fitted = fit_images_to_rpc_budget(pairs)
            img_tags: list[str] = []
            for idx, (url, data) in enumerate(fitted):
                if not data:
                    continue
                img_tags.append(f"图{len(img_tags) + 1}({hashlib.sha256(data).hexdigest()[:8]})")
                content_items.append({
                    "content_type": "image",
                    "data": base64.b64encode(data).decode("ascii"),
                    "mime_type": "image/jpeg",
                })
            line = f"作者:{f.nickname or qq}\n内容:{f.content or '(纯图)'}"
            if img_tags:
                line += "\n" + " ".join(img_tags)
            line += f"\n〔说说ID={f.tid[:12]}〕"
            text_parts.append(line)
        self.ctx.logger.info("QQ空间好友说说查看(qq=%s,%d 条,%d 图)", qq, len(feeds), len(content_items))
        return {"content": "\n\n".join(text_parts), "content_items": content_items}
```

inspect_image：parameters 增 `ToolParameterInfo(name="image_hash", param_type="string", description="图片hash(view_friend_feeds 返回的图标注,8位前缀即可)", required=False)`；签名增 `image_hash: str = ""`；函数开头插入 hash 分支：

```python
        if str(image_hash or "").strip():
            # hash 路径:覆盖非消息来源(view_friend_feeds 等经 tool result media
            # 入库的图片)——database.get 的 filters 精确匹配,取该前缀完整 hash
            prefix = str(image_hash).strip()
            rows = await self.ctx.call_capability(
                "database.get", model_name="Images", filters={}, single_result=False,
            )
            # capability 无 LIKE 时退化为拉表内前缀匹配(Images 表量级有限);
            # 行为不变量:命中唯一才用,多命中/零命中显式报错不猜
            ...
```

实现取表后在插件侧按 `str(row.get("image_hash", "")).startswith(prefix)` 过滤（database.get 返回形态按 :601-606 既有消费方式适配：single_result=False 时取列表）；命中 1 条→复用现有 full_path 读取与 relook 链；0 条→`f"未找到 hash 前缀 {prefix} 对应的图片"`；多命中→报歧义并列出候选前 3。message_id 路径完全不动。

`config.py:224-230` 白名单默认列表追加 `"view_friend_feeds"`（排在 inspect_image 后），描述尾句改为「全域工具(view_friend_feeds/inspect_image)同时在此列,表外工具一律不可用」。

- [ ] **Step 4: 跑测通过**

Run: `python -m pytest tests/test_qzone_wiring.py tests/test_config.py tests/test_qzone_scene.py -q`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(qzone): view_friend_feeds 全域查看工具与 inspect_image hash 路径"
```

---

### Task 8: 说说发布主动触发 + 冷启动种子自举

**Files:**
- Modify: `plugin.py`（类属性区增状态；`_qzone_poll_feeds` 窗口开始块 :1022-1033 与拉取收尾；新增 `_qzone_send_trigger`/`_qzone_seed_virtual_session`）
- Test: `tests/test_qzone_wiring.py`

**Interfaces:**
- Consumes: Task 1 的 `win.get("send_qzone")/("read_qzone")`；`self.ctx.maisaka.proactive.trigger(stream_id=, intent=, reason=, priority="")`（plugin.py:2539 同款）；`_qzone_expected_session_id()`。
- Produces: 状态 `_qzone_send_armed: str`（`"{day}|{start}"`）、`_qzone_send_first_poll_done: bool`；`_qzone_send_trigger(win: dict, *, browsed: bool) -> None`；`_qzone_seed_virtual_session() -> bool`。

- [ ] **Step 1: 写失败测试**

`tests/test_qzone_wiring.py` 追加：

```python
async def test_send_trigger_sendonly_window_fires_immediately():
    """仅 send_qzone 窗口:窗口开始即触发,不等待浏览。"""
    p, ctx = _make_plugin_with_qzone(tmp_path, window={"kind": "daily", "read_qzone": False, "send_qzone": True})
    fired = []
    async def trigger(**kwargs):
        fired.append(kwargs)
        return {"success": True}
    ctx.maisaka.proactive.trigger = trigger
    await p._qzone_poll_feeds()  # 首个 tick:窗口开始→武装→立即触发(浏览轮询照常空转)
    assert len(fired) == 1 and "qzone_post" in fired[0]["intent"]


async def test_send_trigger_cold_start_seed_retry():
    """会话不存在→种子自举→重试一次成功。"""
    p, ctx = _make_plugin_with_qzone(tmp_path)
    calls = {"n": 0, "seed": 0}
    async def trigger(**kwargs):
        calls["n"] += 1
        if calls["n"] == 1:
            return {"success": False, "error": "未找到已存在的聊天流: xxx"}
        return {"success": True}
    async def route(name, msg):
        calls["seed"] += 1
        return {"success": True}
    ctx.maisaka.proactive.trigger = trigger
    ctx.gateway.route_message = route
    await p._qzone_send_trigger({"kind": "daily", "activity": "忙"}, browsed=False)
    assert calls["n"] == 2 and calls["seed"] == 1


async def test_send_trigger_once_per_window():
    """同窗口不重复触发。"""
    p, ctx = _make_plugin_with_qzone(tmp_path, window={"kind": "daily", "read_qzone": False, "send_qzone": True})
    n = {"i": 0}
    async def trigger(**kwargs):
        n["i"] += 1
        return {"success": True}
    ctx.maisaka.proactive.trigger = trigger
    await p._qzone_poll_feeds()
    await p._qzone_poll_feeds()
    assert n["i"] == 1
```

- [ ] **Step 2: 跑测确认失败**

Run: `python -m pytest tests/test_qzone_wiring.py -q`
Expected: FAIL（无触发逻辑）。

- [ ] **Step 3: 实现**

类属性区（:137 `_qzone_session_ids` 附近）增：

```python
    _qzone_send_armed: str = ""        # 发布触发已武装的窗口标记 "{day}|{start}"
    _qzone_send_first_poll_done: bool = False  # 本窗口首轮拉取是否已完成
```

`_qzone_poll_feeds` 窗口开始块（`window_started()` 之后）增：

```python
                day = datetime.now().strftime("%Y-%m-%d")
                self._qzone_send_armed = f"{day}|{win.get('start')}" if win.get("send_qzone") else ""
                self._qzone_send_first_poll_done = False
                if self._qzone_send_armed and not win.get("read_qzone"):
                    # 仅 send 窗口:无浏览上下文,窗口开始即触发
                    self._spawn_background_task(self._qzone_send_trigger(win, browsed=False))
```

拉取流程正常收尾处（充实与入队完成之后、方法 try 块末尾）增：

```python
            if self._qzone_send_armed and not self._qzone_send_first_poll_done:
                self._qzone_send_first_poll_done = True
                if win.get("read_qzone"):
                    # 同窗形态:等首轮浏览注入完成(泵空闲)再触发,分享有上下文
                    self._spawn_background_task(self._qzone_send_trigger(win, browsed=True))
```

新增两个方法（`_qzone_notify_scan` 之前）：

```python
    async def _qzone_send_trigger(self, win: dict, *, browsed: bool) -> None:
        """发布触发:planner 自主决定是否用 qzone_post 发说说(沉默=正常结束)。

        browsed=True(同窗形态)等待首轮浏览注入完成再触发——刚看完好友动态,
        分享有上下文;等待上限 180s,超时也触发(首批已读足够)。
        """
        if browsed:
            deadline = time.monotonic() + 180
            while time.monotonic() < deadline:
                st = self.qzone_injector.stats()
                if not st["p1_queued"] and not st["p2_queued"] and not st["awaiting"]:
                    break
                await asyncio.sleep(5)
        activity = str(win.get("activity") or "")
        if browsed:
            intent = (
                "你刚刷完QQ空间看到了好友的动态,现在有点想分享点什么。"
                "如果确实想发,用 qzone_post 工具(reply_reference 填想表达的方向);"
                f"不想发就保持沉默,什么都不用做。当前活动:{activity}"
            )
        else:
            intent = (
                f"你在忙{activity},忙里偷闲想上QQ空间发条说说。"
                "如果确实想发,用 qzone_post 工具(reply_reference 填想表达的方向);"
                "不想发就保持沉默,什么都不用做。"
            )
        stream_id = self._qzone_expected_session_id()
        if not stream_id:
            self.ctx.logger.warning("QQ空间发布触发跳过:虚拟流 session id 不可得")
            return
        try:
            result = await self.ctx.maisaka.proactive.trigger(
                stream_id=stream_id, intent=intent, reason="日程窗口send_qzone", priority="",
            )
        except Exception:
            self.ctx.logger.exception("QQ空间发布触发失败(虚拟流)")
            result = {"success": False, "error": "异常"}
        if isinstance(result, dict) and result.get("success"):
            self.ctx.logger.info("QQ空间发布触发已发出(browsed=%s)", browsed)
            return
        err_text = str((result or {}).get("error") or "") if isinstance(result, dict) else ""
        if "未找到" in err_text and "聊天流" in err_text:
            # 冷启动自举:虚拟流会话在首条消息进入后才诞生,proactive.trigger
            # 需要已存在会话——注入无 is_mentioned 的种子消息仅建会话后重试一次
            if await self._qzone_seed_virtual_session():
                try:
                    result2 = await self.ctx.maisaka.proactive.trigger(
                        stream_id=stream_id, intent=intent,
                        reason="日程窗口send_qzone(种子自举重试)", priority="",
                    )
                    if isinstance(result2, dict) and result2.get("success"):
                        self.ctx.logger.info("QQ空间发布触发已发出(种子自举,browsed=%s)", browsed)
                        return
                except Exception:
                    self.ctx.logger.exception("QQ空间发布触发重试失败(种子自举后)")
        self.ctx.logger.warning("QQ空间发布触发未成功(%s),等下个窗口", err_text[:60])

    async def _qzone_seed_virtual_session(self) -> bool:
        """冷启动种子:注入一条无 is_mentioned 的 self 消息,仅让主程序创建
        虚拟流会话(不触发 planner 决策轮),供 proactive.trigger 使用。"""
        self._qzone_seq += 1
        bot_uin = str(self.config.favorability.bot_user_id or "").strip()
        msg = {
            "message_id": f"qzone_seed_{int(time.time())}_{self._qzone_seq}",
            "platform": QZONE_PLATFORM,
            "timestamp": str(int(time.time())),
            "message_info": {
                "user_info": {"user_id": bot_uin, "user_nickname": "我"},
                "group_info": {
                    "group_id": self.config.qzone.virtual_group_id,
                    "group_name": self.config.qzone.virtual_group_name,
                },
            },
            "raw_message": [{"type": "text", "data": "(打开了QQ空间)"}],
        }
        try:
            await self.ctx.gateway.route_message(QZONE_GATEWAY_NAME, msg)
            return True
        except Exception:
            self.ctx.logger.exception("QQ空间种子消息注入失败")
            return False
```

- [ ] **Step 4: 跑测通过**

Run: `python -m pytest tests/test_qzone_wiring.py tests/test_integration.py -q`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(qzone): 说说发布主动触发(分窗口形态)与冷启动种子自举"
```

---

### Task 9: 见闻系统（旁路 LLM 唯一路径，窗口结束时生成）

**Files:**
- Create: `prompt_templates/catsitate_qzone_digest.prompt`
- Modify: `catsitate_core/llm_provider.py`（SIDE_TEMPLATES 增 qzone_digest）
- Modify: `catsitate_core/config.py`（digest_enabled/digest_llm_model/digest_llm_timeout_ms）
- Modify: `catsitate_core/qzone/comment_seen.py`（增 fav_events_day）
- Modify: `plugin.py`（快照初始化、窗口结束触发生成、`_qzone_block` 真实流分支）
- Test: `tests/test_qzone_comment_seen.py`、`tests/test_qzone_wiring.py`

**Interfaces:**
- Produces: `CommentSeenStore.fav_events_day(day: str) -> list[dict]`（user_id/kind/text）；插件 `_qzone_digest_snapshot: JsonSnapshot`（`data/qzone_digest.json`，键 date/text）；`_qzone_generate_digest() -> None`；真实流 qzone 注入块优先输出 `[空间见闻] {text}`（当日），无见闻回退现状「近期刷到」。

- [ ] **Step 1: 写失败测试**

`tests/test_qzone_comment_seen.py` 追加：

```python
def test_fav_events_day():
    store = CommentSeenStore(_store(tmp_path))
    store.fav_event("100", "LIKE", "小明 赞了你的说说「x」")
    rows = store.fav_events_day(datetime.now().strftime("%Y-%m-%d"))
    assert len(rows) == 1 and rows[0]["user_id"] == "100" and rows[0]["kind"] == "LIKE"
```

`tests/test_qzone_wiring.py` 追加：

```python
async def test_digest_generated_on_window_end():
    p, ctx = _make_plugin_with_qzone(tmp_path)
    p.qzone_injector.window_started()
    async def llm(messages, model, module, timeout_ms=None):
        return {"success": True, "response": "今天看到小明发的新猫图,还收到两条评论。"}
    p._side_llm_call = llm
    p._qzone_digest_snapshot = JsonSnapshot(tmp_path / "qzone_digest.json")
    await p._qzone_generate_digest()
    data = p._qzone_digest_snapshot.load()
    assert data.get("text", "").startswith("今天看到小明")


def test_qzone_block_prefers_today_digest():
    p, ctx = _make_plugin_with_qzone(tmp_path)
    p._qzone_digest_snapshot = JsonSnapshot(tmp_path / "qzone_digest.json")
    p._qzone_digest_snapshot.save({"date": datetime.now().strftime("%Y-%m-%d"), "text": "见闻正文"})
    qz = p._qzone_block(stream_id="非虚拟流id")
    assert qz and qz[1].startswith("[空间见闻] 见闻正文")
```

- [ ] **Step 2: 跑测确认失败**

Run: `python -m pytest tests/test_qzone_comment_seen.py tests/test_qzone_wiring.py -q`
Expected: FAIL。

- [ ] **Step 3: 实现**

`comment_seen.py` 新增：

```python
    def fav_events_day(self, day: str) -> list[dict]:
        """取某日全部好感度事件(见闻素材:谁与我互动/我做了什么)。"""
        rows = self.store.query(
            "SELECT user_id, kind, text FROM qzone_fav_events WHERE day = ? ORDER BY id", (day,)
        )
        return [{"user_id": r[0], "kind": r[1], "text": r[2]} for r in rows]
```

`config.py` QzoneSection 增（diary 字段后）：

```python
    digest_enabled: bool = _f(True, "空间见闻开关(read_qzone 窗口结束时旁路 LLM 摘要,注入真实聊天)", label="空间见闻开关")
    digest_llm_model: str = _f("memory", "空间见闻摘要模型", label="见闻模型")
    digest_llm_timeout_ms: int = _f(0, "空间见闻超时(毫秒,0=默认)", label="见闻超时(ms)")
```

`llm_provider.py` SIDE_TEMPLATES 增：

```python
    # 空间见闻摘要(read_qzone 窗口结束触发,注入真实聊天的当日空间印象)
    "qzone_digest": {
        "version": 1,
        "system": (
            "你是 bot 的空间记忆助手。根据素材(当天QQ空间浏览到的动态/收到的互动/自己的动作)"
            "写一段「空间见闻」——给 bot 自己日后聊天时引用的简要印象,不是发给谁的消息。\n"
            "要求:\n- 一段话,60~150字,简体中文\n"
            "- 写「谁发了什么/谁和我互动了/我做了什么」的关键印象,可带一句主观感受\n"
            "- 不要列表/标题/引号,直接输出正文"
        ),
    },
```

新建 `prompt_templates/catsitate_qzone_digest.prompt`（同文案）。

`plugin.py`：`_pending_diary_snapshot` 初始化处旁增 `self._qzone_digest_snapshot = JsonSnapshot(<data_dir> / "qzone_digest.json")`；Task 2 改过的窗口结束块在 `window_ended()` 之后增 `self._spawn_background_task(self._qzone_generate_digest())`；新增方法：

```python
    async def _qzone_generate_digest(self) -> None:
        """read_qzone 窗口结束:把当日浏览与互动摘要为见闻。

        主程序会话摘要由 bot 发言后的回写服务生成,虚拟流 receive-only 无
        发言投递,主程序记忆层不会为虚拟流产出内容,插件亦无 API 读取记忆
        段落——故由插件在窗口边界自行摘要(素材→摘要→存储→注入,与主程序
        记忆摘要方法一致)。失败告警并保留上一份。
        """
        if not (self.config.qzone.enabled and self.config.qzone.digest_enabled):
            return
        if not self._qzone_available:
            return
        now = datetime.now()
        day = now.strftime("%Y-%m-%d")
        seen = self.qzone_seen.recent_seen(limit=15, days=1, now=now)
        lines = [
            f"{e['author_nickname'] or e['author_uin']}发了「{(e['summary'] or '图片')[:20]}」"
            for e in seen
        ]
        try:
            events = self.qzone_comment_seen.fav_events_day(day)
        except Exception:
            self.ctx.logger.exception("QQ空间见闻素材(互动事件)读取失败,本轮按空处理")
            events = []
        lines += [e["text"][:40] for e in events[:10]]
        if not lines:
            return  # 当日无素材:不生成,保留旧见闻
        persona, _ = await self._persona_context()
        stable_ctx = [f"bot 人设:{persona}", f"日期:{day}"]
        messages, _ = build_side_prompt("qzone_digest", stable_ctx, ["素材:\n" + "\n".join(lines)])
        try:
            result = await self._side_llm_call(
                messages, self.config.qzone.digest_llm_model, "qzone_digest",
                self.config.qzone.digest_llm_timeout_ms,
            )
        except Exception:
            self.ctx.logger.exception("QQ空间见闻生成失败,保留上一份")
            return
        if not isinstance(result, dict) or not result.get("success"):
            self.ctx.logger.warning("QQ空间见闻 LLM 失败,保留上一份")
            return
        text = str(result.get("response") or "").strip()
        if not text or len(text) > 400:
            self.ctx.logger.warning("QQ空间见闻文本异常(长度 %d),保留上一份", len(text))
            return
        self._qzone_digest_snapshot.save({"date": day, "text": text})
        self.ctx.logger.info("QQ空间见闻已生成(%d 字)", len(text))
```

`_qzone_block`（:1994-2007）真实流分支开头增：

```python
        digest = self._qzone_digest_snapshot.load()
        today = datetime.now().strftime("%Y-%m-%d")
        if digest.get("text") and digest.get("date") == today:
            return f"qzone:d:{today}:{len(digest['text'])}", f"[空间见闻] {digest['text']}"
```

（无当日见闻时走既有「近期刷到」路径不变。）

- [ ] **Step 4: 跑测通过**

Run: `python -m pytest tests/test_qzone_comment_seen.py tests/test_qzone_wiring.py tests/test_config.py -q`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(qzone): 空间见闻——窗口结束旁路摘要并注入真实聊天"
```

---

### Task 10: 点赞通知源C

**Files:**
- Create: `catsitate_core/qzone/like_seen.py`
- Modify: `catsitate_core/qzone/wire.py`（LikeEvent）
- Modify: `catsitate_core/qzone/discovery.py`（parse_like_events）
- Modify: `catsitate_core/qzone/client.py`（get_like_events）
- Modify: `catsitate_core/qzone/seen_store.py`（get_summary）
- Modify: `plugin.py`（on_load 初始化 LikeSeenStore；_qzone_data_prune 增清理；_qzone_notify_scan 增源C）
- Test: `tests/test_qzone_wire.py`、`tests/test_qzone_like_seen.py`（新建）

**Interfaces:**
- Consumes: Task 5 的 `_fetch_likes_raw`；Task 3 的 `format_comment_param_line`/`comment_time_prefix`。
- Produces: `LikeEvent(like_key, liker_uin, liker_nickname, owner_uin, target_tid, create_time)`；`parse_like_events(text) -> list[LikeEvent]`；`client.get_like_events(*, count=30) -> list[LikeEvent]`；`LikeSeenStore(store).is_new(like_key, *, liker_uin, target_tid) -> bool`（发现即登记）+ `prune(days=30, now=None) -> int`；`SeenStore.get_summary(tid) -> str`。

- [ ] **Step 1: 写失败测试**

新建 `tests/test_qzone_like_seen.py`：

```python
from catsitate_core.qzone.like_seen import LikeSeenStore


def test_like_seen_dedup_and_prune(tmp_path):
    from catsitate_core.storage import SQLiteStore
    store = LikeSeenStore(SQLiteStore(tmp_path / "t.db"))
    assert store.is_new("100_200_ab12", liker_uin="100", target_tid="t1") is True
    assert store.is_new("100_200_ab12", liker_uin="100", target_tid="t1") is False
    assert store.is_new("101_200_ab12", liker_uin="101", target_tid="t1") is True
```

`tests/test_qzone_wire.py` 追加：

```python
def test_parse_like_events_anchors():
    from catsitate_core.qzone.discovery import parse_like_events
    html = (
        '<li class="fn_like" data-key="100_200_ab12cd34" data-tid="ee3396c49d38">'
        '<a data-uin="100" href="#">小明</a>赞了你的说说</li>'
    )
    events = parse_like_events(html)
    assert len(events) == 1
    ev = events[0]
    assert ev.like_key == "100_200_ab12cd34" and ev.liker_uin == "100"
    assert ev.owner_uin == "200" and ev.target_tid == "ee3396c49d38"
    assert ev.liker_nickname == "小明"
```

- [ ] **Step 2: 跑测确认失败**

Run: `python -m pytest tests/test_qzone_like_seen.py tests/test_qzone_wire.py -q`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 实现**

`wire.py` 新增：

```python
@dataclass
class LikeEvent:
    """「与我相关」流中的赞事件(键=赞的人_说说主人_说说哈希)。"""

    like_key: str
    liker_uin: str
    liker_nickname: str
    owner_uin: str
    target_tid: str
    create_time: str = ""  # epoch 秒字符串,缺失为空(不编造时间)
```

`discovery.py` 新增：

```python
LIKE_EVENT_RE = re.compile(
    r'data-key="(?P<liker>\d+)_(?P<owner>\d+)_(?P<hash>[0-9a-fA-F]+)"[^>]*'
    r'data-tid="(?P<tid>[0-9a-fA-F]+)"(?P<body>.*?)(?=data-key="|$)',
    re.DOTALL,
)
LIKE_UIN_RE = re.compile(r'data-uin="(?P<uin>\d+)"[^>]*>(?P<nick>[^<]{0,24})<')


def parse_like_events(text: str) -> list[LikeEvent]:
    """解析「与我相关」流(scope=1)中的赞事件。

    锚点:事件块 data-key="{liker}_{owner}_{hash}" + 目标 data-tid,
    块内 data-uin 锚定点赞者昵称。解析锚点来自协议调查;实机响应若与
    锚点不符,零事件属正常(无人点赞),格式漂移由调用方按响应规模告警。
    """
    events: list[LikeEvent] = []
    for m in LIKE_EVENT_RE.finditer(text):
        body = m.group("body") or ""
        uin_match = LIKE_UIN_RE.search(body)
        liker_uin = uin_match.group("uin") if uin_match else m.group("liker")
        nickname = (uin_match.group("nick").strip() if uin_match else "") or liker_uin
        events.append(LikeEvent(
            like_key=f"{m.group('liker')}_{m.group('owner')}_{m.group('hash')}",
            liker_uin=liker_uin, liker_nickname=nickname,
            owner_uin=m.group("owner"), target_tid=m.group("tid"),
        ))
    return events
```

`client.py` 新增：

```python
    async def get_like_events(self, *, count: int = 30) -> list[LikeEvent]:
        """拉取「与我相关」赞事件(feeds3_html_more?scope=1)。"""
        text = await self._fetch_likes_raw(count=count)
        return parse_like_events(text)
```

`seen_store.py` 新增：

```python
    def get_summary(self, tid: str) -> str:
        """按 tid 取已注入说说的摘要(通知 quote/点赞标题素材)。"""
        rows = self.store.query("SELECT summary FROM qzone_feeds WHERE tid = ?", (tid,))
        return str(rows[0][0]) if rows else ""
```

新建 `catsitate_core/qzone/like_seen.py`：

```python
"""赞事件去重(qzone_likes):同一人同一条说说只通知一次。

键=liker_owner_hash(取消赞再赞不重复通知);30 天修剪防表无限增长。
"""

from __future__ import annotations

import sqlite3
from datetime import datetime, timedelta


class LikeSeenStore:
    def __init__(self, store) -> None:
        self.store = store
        self.store.execute(
            "CREATE TABLE IF NOT EXISTS qzone_likes ("
            "like_key TEXT PRIMARY KEY, liker_uin TEXT NOT NULL, "
            "target_tid TEXT NOT NULL, created_at TEXT NOT NULL)"
        )

    def is_new(self, like_key: str, *, liker_uin: str, target_tid: str) -> bool:
        """发现即登记(与评论去重同契约):新事件 True,重复 False。"""
        try:
            self.store.execute(
                "INSERT INTO qzone_likes (like_key, liker_uin, target_tid, created_at) "
                "VALUES (?, ?, ?, ?)",
                (like_key, liker_uin, target_tid, datetime.now().strftime("%Y-%m-%dT%H:%M:%S")),
            )
            return True
        except sqlite3.IntegrityError:
            return False

    def prune(self, days: int = 30, now: datetime | None = None) -> int:
        """清理过期赞事件记录。"""
        cutoff = (now or datetime.now()) - timedelta(days=days)
        before = self.store.query("SELECT COUNT(*) FROM qzone_likes")[0][0]
        self.store.execute(
            "DELETE FROM qzone_likes WHERE created_at < ?",
            (cutoff.strftime("%Y-%m-%dT%H:%M:%S"),),
        )
        after = self.store.query("SELECT COUNT(*) FROM qzone_likes")[0][0]
        return before - after
```

`plugin.py`：on_load 处 `self.qzone_comment_seen = ...` 旁增 `self.qzone_like_seen = LikeSeenStore(self.store)`（补 import）；`_qzone_data_prune` 内增 `self.qzone_like_seen.prune(days=30)`（该任务已有 comment_seen.prune 同款调用形态）。`_qzone_notify_scan` 源B 之后、入队之前增源C：

```python
            # ---- 源C:有人赞了我的说说(feeds3_html_more?scope=1) ----
            if len(notifications) < 3:
                try:
                    likes = await self.qzone_client.get_like_events(count=30)
                except QzoneAuthError:
                    self.qzone_cookie.invalidate()
                    self.ctx.logger.warning("QQ空间登录态失效(通知轮询源C),cookie 已作废,下轮重取")
                    likes = []
                except Exception:
                    self.ctx.logger.exception("QQ空间通知轮询源C失败,本轮跳过源C")
                    likes = []
                for ev in likes:
                    if str(ev.liker_uin) == bot_uin or not ev.target_tid:
                        continue
                    if not self.qzone_like_seen.is_new(
                        ev.like_key, liker_uin=str(ev.liker_uin), target_tid=str(ev.target_tid)
                    ):
                        continue
                    try:
                        like_epoch = float(str(ev.create_time or "").strip())
                    except ValueError:
                        like_epoch = 0.0
                    if like_epoch > 0 and like_epoch < stale_before:
                        continue
                    origin_summary = self.qzone_seen.get_summary(str(ev.target_tid))
                    time_tag = comment_time_prefix(ev.create_time, now_epoch)
                    param = "〔说说ID=" + str(ev.target_tid)[:12]
                    if time_tag:
                        param += f" 点赞于{time_tag}"
                    param += "〕"
                    title = f"「{origin_summary[:20]}」" if origin_summary else ""
                    notifications.append(FeedItem(
                        tid=f"notify_like_{ev.like_key}",
                        abstime=ev.create_time, uin=str(ev.liker_uin),
                        nickname=ev.liker_nickname or str(ev.liker_uin),
                        content=f"赞了你的说说{title}\n{param}",
                        source="notify", dedup_key=ev.like_key,
                        origin_tid=str(ev.target_tid), origin_content=origin_summary,
                        origin_sender=bot_uin,
                    ))
                    try:
                        self.qzone_comment_seen.fav_event(
                            str(ev.liker_uin), "LIKE",
                            f"{ev.liker_nickname or ev.liker_uin} 赞了你的说说{title or '(我的内容)'}",
                        )
                    except Exception:
                        self.ctx.logger.exception("QQ空间赞事件好感度记账失败(仅告警)")
                    if len(notifications) >= 3:
                        break
```

- [ ] **Step 4: 跑测通过**

Run: `python -m pytest tests/test_qzone_like_seen.py tests/test_qzone_wire.py tests/test_qzone_wiring.py -q`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(qzone): 点赞通知源C(scope=1 解析+去重表+P1 注入)"
```

---

### Task 11: 日记人设前置

**Files:**
- Modify: `plugin.py:2416-2420`（stable_ctx 前置人设）
- Modify: `catsitate_core/llm_provider.py`（qzone_diary version 2）
- Modify: `prompt_templates/catsitate_qzone_diary.prompt`
- Test: `tests/test_llm_provider.py`、`tests/test_integration.py`

**Interfaces:**
- Consumes: `_persona_context()`。
- Produces: 日记 stable_ctx 首段 `bot 人设:{persona}`；模板指示「你就是这位用户本人，以人设口吻书写」。

- [ ] **Step 1: 写失败测试**

`tests/test_llm_provider.py` 追加：

```python
def test_qzone_diary_template_mentions_persona():
    system, _ = load_side_system("qzone_diary")  # 无部署文件时取内置默认
    assert "人设" in system and "本人" in system
```

`tests/test_integration.py` 追加（沿用该文件 stub 构造）：

```python
async def test_diary_stable_ctx_includes_persona():
    p = _make_plugin(tmp_path)
    captured = {}

    async def side_llm(messages, model, module, timeout_ms=None):
        captured["messages"] = messages
        return {"success": True, "response": "今天很平静。" * 5}

    async def publish(*, content):
        return "tid123"

    p._side_llm_call = side_llm
    p.qzone_client.do_publish = publish
    await p._generate_and_publish_diary()
    assert captured["messages"][1]["content"].startswith("bot 人设:")
```

- [ ] **Step 2: 跑测确认失败**

Run: `python -m pytest tests/test_llm_provider.py tests/test_integration.py -q`
Expected: FAIL。

- [ ] **Step 3: 实现**

`plugin.py:2416-2420` 替换为：

```python
        persona, _ = await self._persona_context()
        stable_ctx = (
            f"bot 人设:{persona}\n"
            f"今天的日程:{schedule_summary or '自由活动'}\n"
            f"备忘:{memos or '无'}\n看到的好友动态:{seen_summary or '无'}"
        )
        messages, _ = build_side_prompt("qzone_diary", [stable_ctx], [])
```

`llm_provider.py` qzone_diary：`"version": 1` → `2`，system 首行改为：

```python
            "你就是这位用户本人(人设见素材首段),在睡前写一篇今日日记并发布为空间说说。\n"
            "以人设的身份、口吻和习惯用词书写,像你自己随手写的。\n"
```

（其余要求行保留。）`prompt_templates/catsitate_qzone_diary.prompt` 同步改写（首两句同上，其余要求不变）。

- [ ] **Step 4: 跑测通过**

Run: `python -m pytest tests/test_llm_provider.py tests/test_integration.py -q`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(qzone): 日记生成人设前置(以人设口吻书写)"
```

---

### Task 12: 场景文案、白名单与文档收尾

**Files:**
- Modify: `catsitate_core/qzone/scene.py:23-28`（QZONE_SCENE_TEXT v4）
- Modify: `prompt_templates/catsitate_qzone_scene.prompt`、`catsitate_core/llm_provider.py`（qzone_scene version 4）
- Modify: `docs/plugin-manual.md`、`CONTEXT.md`、`CHANGELOG.md`、`_manifest.json`（版本 0.8.0）、`docs/superpowers/specs/milestone-map.md`、`docs/superpowers/specs/2026-09-01-qzone-full-design.md`（§4.1 hash 显示口径微调）
- Delete: `docs/superpowers/plans/2026-09-01-qzone-m3-fix.md`（被本计划取代）
- Test: `tests/test_qzone_scene.py`（文案断言更新）、全量

**Interfaces:**
- Produces: 场景文案对齐两段式工具参数与点赞通知；版本 0.8.0；文档与配置全量对齐设计稿。

- [ ] **Step 1: 更新场景文案测试**

`tests/test_qzone_scene.py` 中对 QZONE_SCENE_TEXT 的断言改为包含 `reply_reference`、`qzone_post`、`赞`：

```python
def test_scene_text_matches_two_stage_tools():
    assert "reply_reference" in QZONE_SCENE_TEXT
    assert "qzone_like" in QZONE_SCENE_TEXT and "qzone_post" in QZONE_SCENE_TEXT
```

- [ ] **Step 2: 跑测确认失败**

Run: `python -m pytest tests/test_qzone_scene.py -q`
Expected: FAIL。

- [ ] **Step 3: 实现**

`scene.py` QZONE_SCENE_TEXT（SIDE_TEMPLATES qzone_scene `version: 4` 与 prompt 文件同步）：

```python
QZONE_SCENE_TEXT = (
    "你正在用手机刷QQ空间。消息流里有好友的说说动态和互动通知"
    "(有人评论/回复/赞了你,引用的那条就是被评论的说说)。\n"
    "每条消息底部〔〕括号里的是工具参数:说说ID填 qzone_comment/qzone_reply/qzone_like 的 feed_id,"
    "评论ID填 qzone_reply 的 comment_id,评论者QQ填 qzone_comment 的 at_user_id。\n"
    "想互动就调工具:评论调 qzone_comment(reply_reference 填你想表达的方向,正文会按你的口吻生成);"
    "回复评论调 qzone_reply(填说说ID、评论ID和 reply_reference);点赞调 qzone_like;"
    "想分享自己的心情就调 qzone_post(reply_reference 填想分享什么,正文会按你的口吻生成)。\n"
    "不感兴趣就保持沉默,什么都不用做。在这里直接打字是发不出去的,动作只能通过工具完成。"
)
```

文档收尾：
- `plugin-manual.md`：新配置（discovery_count/discovery_max_pages/own_feed_scan_count/expression_llm_*/digest_*）、工具签名变化（reply_reference 两段式、view_friend_feeds、inspect_image image_hash）、点赞通知、发布主动触发（两形态+种子自举）、见闻块、通知独立于窗口。
- `CONTEXT.md`：术语补「表达生成层/reply_reference/见闻/源C/种子自举」。
- `CHANGELOG.md`：0.8.0 条目（自包含描述，按用户视角写功能）。
- `_manifest.json`：version → 0.8.0。
- `milestone-map.md`：M3 修正各项标记完成。
- 设计稿 §4.1：hash 摘要行改为「图1(ab12cd34)」为 8 位前缀显示、inspect_image 按前缀匹配 Images 表（与 Task 7 实现一致）；§9 tool_whitelist 默认值同步含 view_friend_feeds。
- 删除被取代的旧计划：`git rm docs/superpowers/plans/2026-09-01-qzone-m3-fix.md`。

- [ ] **Step 4: 全量测试**

Run: `cd /home/hesitate-p/Catsitate/MaiBot-dev/plugins/catsitate_core_maibot && python -m pytest tests/ -q`
Expected: 全绿（基线 402+ 新增）。

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "chore(qzone): 场景文案两段式对齐与 0.8.0 文档收尾"
```

---

## Self-Review 记录

- 规格覆盖：§2 架构（表达生成层→Task 6；发布触发→Task 8）、§3.1 时间（Task 3/10）、§3.2 翻页（Task 5）、§3.4 三源+独立注入（Task 2/10）、§4.1 工具（Task 6/7）、§4.2 表达层（Task 6）、§4.4 触发+自举（Task 8）、§4.5 登记三处（Task 4/7+泵既有）、§5 日记（Task 11 + Task 4 tid）、§6 属性分离（Task 1）、§7 见闻（Task 9）、§8 qzone_likes（Task 10）、§9 配置（Task 5/6/9/12）——全覆盖。
- 联调确认点（实机验收时校准，均不阻塞离线开发）：① extract_publish_tid 的 tid 键形态（Task 4）；② parse_like_events 锚点正则（Task 10）；③ database.get 单表拉取形态（Task 7 hash 路径）。
- 已知裁剪：I8（replyer 浪费）经实证撤销——replyer 仅在 planner 调 reply 工具时运行，虚拟流白名单不放行 reply，无浪费，无改动项。
