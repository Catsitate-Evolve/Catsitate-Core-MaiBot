# CONTEXT — Catsitate 领域词汇表

> 只收录已对齐的领域术语(二期设计 grilling 固化;QQ空间条目为三期 M1/M2 对齐),不含实现细节。

## 日程与睡眠

- **日程**:bot 一天的窗口排布(1 个睡眠窗口 + 1~8 个活动窗口,按活动排列,数量与时间段由 LLM 生成时自主决定,可经工具增删改)。
- **日程窗口**:日程的基本单元,分两类:
  - **睡眠窗口**:唯一可入睡的窗口(恰好 1 个),时间段 = 入睡时刻~醒来时刻;
  - **活动窗口**:按活动排列(1~8 个,数量由 LLM 生成或工具增删),含时间段 + 活动描述 + 是否计划发言 + 发言主题 + `kind` 标注(greeting 问候类 / daily 日常)。
- **睡眠窗口(可入睡时间)**:睡觉时段 = 窗口起点 ~ 窗口终点(计划醒来时刻,到点自然醒;默认 23:00-7:30 为软基准)。窗口即**可入睡时间**,窗口内允许入睡:晚安判定入睡(与静默开关无关)/ 静默入睡(静默开启时安静满 N 分钟,计时基准 = max(窗口起点, 最后活动));静默关闭则窗口起点直接入睡。窗口终点仍未入睡 → 不入睡,但补执行入睡时的任务(生成次日日程,每窗口一次)。睡眠窗口外无入睡通路,提前入睡不得侵占其它日程;活动与睡眠重叠时睡眠被压缩(入睡推迟、醒来不变,醒来是作息锚点),压缩不足最短睡眠则拒绝;跨午夜时旧日程的活跃睡眠窗口保留至窗口结束。
- **绝对静默**:睡眠期间 bot 不执行任何操作——拦截一切入站消息(含命令)、@ 不唤醒、提醒不执行;唯一例外是次日日程生成(入睡确认瞬间,或睡眠窗口终点未入睡时补执行)。
- **默认作息模板**:内置兜底作息(23:00 睡 / 07:30 起)。用于:首日无日程撑场、日程生成失败撑场。入睡生成与睡眠窗口终点补生成是仅有的两条日程生成路径。
- **入睡确认**:入睡状态切换成功的瞬间,唯一触发次日日程生成的时机。
- **晚安判定器**:对出站晚安短句做 `SLEEP / NOT_SLEEP / UNSURE` 三值判定,`SLEEP` 才入睡(防诱导:只接受短句、自我入睡)。

## 好感度

- **互动**:流内最近一次 bot 消息(回应或主动发言)的时间;用于自然衰减计时。按人跨流聚合(取所有活跃流中最近互动时间)。
- **自然衰减**:超过 N 天无互动后,由 LLM 判定衰减分(0~-decay_max)与拟人化新注记。
- **发言门槛**:主动发言的最低好感度等级,按人好感度判定:日常发言=熟悉(配置 `speak_threshold_level`);主动问候=特别(与独占等级绑定,无独立配置)。

## 备忘录

- **memo 按人**:备忘条目的人维度组织(M2 重构,spec §3.10)——主 QQ + 附带 QQ 列表(≤5),任一牵连 QQ 命中当前对话对象即可见(私聊/群聊/虚拟流跨流);流维度保留为元数据与命中条件之一,不再是唯一归属。
- **备忘提醒(remind_at)**:备忘的可选绝对提醒时间,仅与日程联动。
- **提醒两级**:①日程收录(生成日程时并入对应窗口)②独立兜底(无日程时到点注入备忘归属流)。睡眠期间两级都不执行。

## 发言

- **执行判定**:每个日程窗口触发时的一次 LLM 调用,结合当前窗口活动全文 + 全天概览,输出「是否发言 + 发言文本」;计划发言(plan_speak)仅作先验,执行 LLM 拥有最终决定权。
- **主动发言**:bot 不受消息触发、由日程窗口驱动的发言;发送目标每次单流(LLM 选最值得说的流)。
- **主动问候**:好感度达「特别」级(全表唯一)的用户,须存在私聊流,在 greeting 窗口起点收到的主动问候;无每日一次限制。

## QQ空间

- **qzone 属性**:日程活动窗口的可选标记(true/false),仅 daily 窗口可标记的空间浏览开关——标记的窗口内 bot 会刷QQ空间(拉取好友动态并注入虚拟流);非 daily 窗口的 qzone 标记非法(校验拒绝/钳制清除)。
- **虚拟流**:platform=`qzone-qq` 的伪群聊流——QQ空间动态以消息形式注入此流,复用主程序 planner→replyer 链;person 经主程序连字符别名机制(含 `-` 的平台名取连字符后段命名空间,split 后第 2 段,如 qzone-qq → qq)与真实 QQ 聊天统一为同一人,路由/账号仍按 `qzone-qq` 原始字符串分键。
- **seen/interacted**:动态两层状态——注入成功(进入过 planner 上下文)= seen;点赞/评论过 = interacted(M2)。seen ≠ 互动;窗口结束仍未注入的 queued 行回退未读。
- **场景替换**:读主程序 `chat.reply_style.group_chat_prompt` 当前值,在 planner 与 replyer 两侧的 system 文本中**原位精确替换**为QQ空间场景文案(按配置值匹配,用户改过配置也能命中);未命中/配置为空则告警并回退注入块语义说明。
- **出站意图**:注入侧在注入动态/评论事件前**前置设定**的路由意图,驱动回调据此把虚拟流出站文本发为真实动作——好友说说→空间评论、好友评论→楼中楼回复;**一次性消费**(首个动作成功即置空,后续出站按无意图拒发),窗口结束未消费即作废。
- **评论轮询**:浏览窗口外(醒着且不在 qzone 窗口)的周期任务,**仅拉取自己说说下的新评论**——一条一意(等 bot 回复消费后再取下一条),好友新动态窗口外不感知;睡眠期绝对静默拦下。
- **发现层**:统一时间线浏览的第一段——`feeds3_html_more` 1 次调用即得全好友动态索引,与好友总数无关。
- **充实层**:发现层之后的第二段——仅对发现新动态的好友按人拉取完整说说内容(1+N 次调用,N=有新动态的作者数)。
- **FeedDiscovery**:发现层条目的轻量索引形态(仅 tid/uin/nickname/abstime/appid 五字段),不含正文图片,供过滤与分组,完整实体由充实层补齐。
