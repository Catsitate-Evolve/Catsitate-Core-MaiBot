# QQ空间模块 M2（互动）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付三期 M2「互动」：虚拟流出站经意图状态机路由为**真实空间评论/楼中楼回复**；`qzone_like` 点赞工具；窗口外评论轮询（好友在 bot 说说下的新评论→注入→回复）；好感度显式事件（评论双向计入结算素材+衰减计时）；memo 按人重构（主QQ+附带QQ，跨流可见）。

**Architecture:** 写路径全部经 `QzoneClient`（httpx form-POST，Maizone 实证参数集）；出站意图由插件在**注入侧**前置设定（feed 注入→reaction / 评论注入→comment_reply），duplex 驱动回调按意图调 API；评论轮询为独立周期任务（醒着且不在 qzone 窗口时拉自己说说的 commentlist）；好感度走新事件表（不依赖 batch_counter）；memo 加 `extra_user_ids` 列实现人维度跨流。

**Tech Stack:** 同 M1（Python 3.13 / maibot-plugin-sdk / httpx / sqlite3 / pytest）。

**Spec:** `docs/superpowers/specs/2026-08-30-phase3-qzone-design.md` §2.4/§2.7/§3.3/§3.6/§3.7/§3.9/§3.10/§5（M2 增量配置）。**注意 M1 联调修正已固化的语义**：时间=方案 B（timestamp=阅读时刻/前缀=发布时间）、拉取=逐好友 msglist、图片=napcat-adapter 形态、无行为护栏（动作 API 失败不重试循环）。

## Global Constraints

- 只改插件目录；简体中文；错误显式暴露（失败告警不静默、不重试循环——写路径 `max_retries` 生效语义=M2 动作 API 仍 0 重试）。
- 只用 SDK 声明 API（写路径经插件自身 httpx，无新 capability）；测试无网络无主程序（IO 全注入）。
- **写路径有真实副作用**（真实评论/点赞发布到 QQ 空间）——联调验收由用户在自己测试账号上进行；实现期全部走 fake。
- 时间语义方案 B：评论轮询注入消息 timestamp=注入时刻、is_mentioned=1.0、`message_info.additional_config`。
- 版本 0.4.0→0.5.0（`_manifest.json`+`CHANGELOG.md` 同步）。

---

### Task 1: 写路径纯函数（表单构造 + 评论解析）

**Files:**
- Create: `catsitate_core/qzone/wire.py`（写路径纯函数集中地）
- Test: `tests/test_qzone_wire.py`

**Interfaces:**
- Produces（Task 2/3 消费，签名逐字遵守）:
  - `@dataclass CommentItem: comment_tid: str; uin: str; nickname: str; content: str; create_time: str`（楼中楼暂不解析——msglist 顶层 commentlist 即够,`list_3` 深层留 M3 观察）
  - `build_like_form(*, fid: str, target_qq: str, bot_uin: str, now_epoch: float) -> dict`
  - `build_comment_form(*, fid: str, target_qq: str, bot_uin: str, content: str) -> dict`
  - `build_reply_form(*, fid: str, target_qq: str, bot_uin: str, comment_tid: str, comment_uin: str, comment_nick: str, content: str) -> dict`
  - `parse_feed_comments(payload: dict) -> dict[str, list[CommentItem]]`（feed_tid → 该说说下的评论列表）

- [ ] **Step 1: 失败测试**（Maizone 实证参数集为基准,逐字段断言）

```python
"""写路径纯函数测试:表单构造(Maizone 实证参数集)/评论解析。"""
import json

from catsitate_core.qzone.wire import (
    CommentItem, build_comment_form, build_like_form, build_reply_form, parse_feed_comments,
)


def test_build_like_form():
    form = build_like_form(fid="tidA", target_qq="8888", bot_uin="3545773341", now_epoch=1750000000.0)
    assert form["unikey"] == "http://user.qzone.qq.com/8888/mood/tidA"
    assert form["curkey"] == form["unikey"]
    assert form["opuin"] == "3545773341" and form["fid"] == "tidA"
    assert form["qzreferrer"] == "https://user.qzone.qq.com/3545773341"
    assert form["appid"] == 311 and form["format"] == "json" and int(form["abstime"]) == 1750000000


def test_build_comment_form():
    form = build_comment_form(fid="tidA", target_qq="8888", bot_uin="3545773341", content="好看!")
    assert form["topicId"] == "8888_tidA__1"
    assert form["hostUin"] == "8888" and form["uin"] == "3545773341"
    assert form["content"] == "好看!" and form["feedsType"] == 100 and form["format"] == "fs"


def test_build_reply_form():
    form = build_reply_form(fid="tidA", target_qq="8888", bot_uin="3545773341",
                            comment_tid="777", comment_uin="10001", comment_nick="小明", content="谢谢")
    assert form["topicId"] == "8888_tidA__1"
    assert form["commentId"] == "777" and form["commentUin"] == "10001"
    assert form["content"] == "@{uin:10001,nick:小明,auto:1}谢谢"
    assert form["paramstr"] == "1"


def test_parse_feed_comments():
    payload = json.loads('_CB_(' + json.dumps({
        "code": 0,
        "msglist": [
            {"tid": "tidA", "content": "我的说说", "commentlist": [
                {"tid": "c1", "uin": 10001, "name": "小明", "content": "第一条", "create_time": 1750000001},
                {"tid": 2, "uin": 10002, "name": "小红", "content": "第二条", "create_time": 1750000002},
            ]},
            {"tid": "tidB", "content": "无评论", "commentlist": None},
        ],
    }, ensure_ascii=False) + ')')  # 直接构造 dict,不走截取
    out = parse_feed_comments(payload)
    assert set(out) == {"tidA"} and len(out["tidA"]) == 2
    c1 = out["tidA"][0]
    assert (c1.comment_tid, c1.uin, c1.nickname, c1.content, c1.create_time) == (
        "c1", "10001", "小明", "第一条", "1750000001")
    assert out["tidA"][1].comment_tid == "2"  # 数值 tid 归一为字符串


def test_parse_feed_comments_empty():
    assert parse_feed_comments({"code": 0, "msglist": None}) == {}
```

- [ ] **Step 2:** `python3 -m pytest tests/test_qzone_wire.py -q` → FAIL（模块不存在）
- [ ] **Step 3: 实现** `catsitate_core/qzone/wire.py`

```python
"""QQ 空间写路径纯函数(参数集为 Maizone qzone_api.py 实证,联调期经 jsdelivr 复核)。

comment/reply 响应为 format=fs 的 frameElement.callback 包裹——复用
protocol.extract_callback_json 通用截取。仅纯函数,IO 在 client.py。
"""

from __future__ import annotations

from dataclasses import dataclass


@dataclass
class CommentItem:
    """自己说说下的一条好友评论(msglist.commentlist 条目)。"""

    comment_tid: str
    uin: str
    nickname: str
    content: str
    create_time: str


def build_like_form(*, fid: str, target_qq: str, bot_uin: str, now_epoch: float) -> dict:
    """点赞 internal_dolike_app 的表单(unikey/curkey 为动态唯一标识,appid=311)。"""
    unikey = f"http://user.qzone.qq.com/{target_qq}/mood/{fid}"
    return {
        "qzreferrer": f"https://user.qzone.qq.com/{bot_uin}",
        "opuin": bot_uin,
        "unikey": unikey,
        "curkey": unikey,
        "appid": 311,
        "from": 1,
        "typeid": 0,
        "abstime": int(now_epoch),
        "fid": fid,
        "active": 0,
        "format": "json",
        "fupdate": 1,
    }


def build_comment_form(*, fid: str, target_qq: str, bot_uin: str, content: str) -> dict:
    """评论 emotion_cgi_re_feeds 的表单(topicId={host}_{fid}__1,feedsType=100,format=fs)。"""
    return {
        "topicId": f"{target_qq}_{fid}__1",
        "uin": bot_uin,
        "hostUin": target_qq,
        "feedsType": 100,
        "inCharset": "utf-8",
        "outCharset": "utf-8",
        "plat": "qzone",
        "source": "ic",
        "platformid": 52,
        "format": "fs",
        "ref": "feeds",
        "content": content,
    }


def build_reply_form(*, fid: str, target_qq: str, bot_uin: str, comment_tid: str,
                     comment_uin: str, comment_nick: str, content: str) -> dict:
    """楼中楼回复表单(同评论端点 + commentId/commentUin;@ 前缀为 QQ 空间回复格式)。"""
    form = build_comment_form(fid=fid, target_qq=target_qq, bot_uin=bot_uin, content=content)
    form["content"] = f"@{{uin:{comment_uin},nick:{comment_nick},auto:1}}{content}"
    form["commentId"] = str(comment_tid)
    form["commentUin"] = str(comment_uin)
    form["richtype"] = ""
    form["richval"] = ""
    form["paramstr"] = "1"
    return form


def parse_feed_comments(payload: dict) -> dict[str, list[CommentItem]]:
    """解析 msglist 载荷的 commentlist → {feed_tid: [CommentItem]}。

    无评论/缺字段容错跳过;数值 tid 归一为字符串。
    """
    out: dict[str, list[CommentItem]] = {}
    for feed in (payload or {}).get("msglist") or []:
        if not isinstance(feed, dict):
            continue
        tid = str(feed.get("tid") or "")
        items: list[CommentItem] = []
        for c in feed.get("commentlist") or []:
            if not isinstance(c, dict):
                continue
            uin = str(c.get("uin") or "")
            if not uin:
                continue
            items.append(CommentItem(
                comment_tid=str(c.get("tid") or ""),
                uin=uin,
                nickname=str(c.get("name") or "") or uin,
                content=str(c.get("content") or "").strip(),
                create_time=str(c.get("create_time") or ""),
            ))
        if tid and items:
            out[tid] = items
    return out
```

- [ ] **Step 4:** 测试 PASS
- [ ] **Step 5:** `git add -A && git commit -m "feat(qzone): 写路径纯函数——点赞/评论/楼中楼表单构造与评论解析(Maizone 实证参数集)"`

---

### Task 2: QzoneClient 写方法

**Files:**
- Modify: `catsitate_core/qzone/client.py`（`_request` 已支持 data 形参——核对当前签名,如无则补）
- Test: `tests/test_qzone_client.py`（追加）

**Interfaces:**
- Consumes: Task 1 全部。
- Produces: `async def do_like(self, *, fid: str, target_qq: str) -> bool`；`async def do_comment(self, *, fid: str, target_qq: str, content: str) -> bool`；`async def do_reply(self, *, fid: str, target_qq: str, comment_tid: str, comment_uin: str, comment_nick: str, content: str) -> bool`；`async def get_own_feed_comments(self, *, bot_uin: str, num: int = 10) -> tuple[dict[str, list[CommentItem]], dict[str, str]]`（评论映射 + {feed_tid: feed_content} 上下文）。失败抛出/返回 False 由调用方告警,**不重试**（`max_retries` 语义=动作 API 失败即告警跳过）。

- [ ] **Step 1: 失败测试**（追加;fake fetch 断言 POST/表单/headers/响应校验）

```python
# ---- 写路径(fake 注入,无网络) ----
from catsitate_core.qzone.wire import CommentItem


def _post_client(responses):
    cookies = {"p_skey": "SK", "uin": "o3545773341"}
    seen = []

    async def fake_cookie():
        return cookies

    async def fake_fetch(method, url, *, params, headers, timeout_ms, data=None):
        seen.append({"method": method, "url": url, "params": dict(params), "data": dict(data or {}), "headers": dict(headers)})
        return responses.pop(0)

    client = QzoneClient(cookie_provider=fake_cookie, fetch=fake_fetch, timeout_ms=1000, max_retries=0)
    return client, seen


def test_do_like_posts_form_and_parses_plain_json():
    client, seen = _post_client([(200, b'{"code":0}')])
    assert asyncio.run(client.do_like(fid="tidA", target_qq="8888")) is True
    req = seen[0]
    assert req["method"] == "POST"
    assert "internal_dolike_app" in req["url"]
    assert req["params"]["g_tk"] == generate_gtk("SK")
    assert req["data"]["unikey"].endswith("/mood/tidA")
    assert req["headers"]["Origin"] == "https://user.qzone.qq.com"


def test_do_comment_parses_fs_wrapper():
    client, seen = _post_client([(200, 'frameElement.callback({"code":0,"subcode":0});'.encode())])
    assert asyncio.run(client.do_comment(fid="tidA", target_qq="8888", content="好看!")) is True
    assert "emotion_cgi_re_feeds" in seen[0]["url"]
    assert seen[0]["data"]["topicId"] == "8888_tidA__1"


def test_write_failure_raises_no_retry():
    client, seen = _post_client([(200, b'{"code":-3}"}')])  # 业务错
    try:
        asyncio.run(client.do_comment(fid="t", target_qq="8", content="x"))
        raised = False
    except RuntimeError:
        raised = True
    assert raised and len(seen) == 1  # 不重试


def test_write_auth_error_invalidates_cookie():
    from catsitate_core.qzone.client import QzoneAuthError

    async def fake_cookie():
        return {"p_skey": "SK"}

    calls = []

    async def fake_fetch(method, url, *, params, headers, timeout_ms, data=None):
        calls.append(1)
        return 200, '_CB({"code":-3000})'.encode()

    invalidated = []

    class _Client(QzoneClient):
        async def do_comment(self, **kw):
            try:
                return await super().do_comment(**kw)
            except QzoneAuthError:
                invalidated.append(1)
                raise

    client = _Client(cookie_provider=fake_cookie, fetch=fake_fetch, timeout_ms=1000, max_retries=0)
    try:
        asyncio.run(client.do_comment(fid="t", target_qq="8", content="x"))
    except QzoneAuthError:
        pass
    assert invalidated == [1] and len(calls) == 1


def test_get_own_feed_comments():
    body = b'_preloadCallback({"code":0,"msglist":[{"tid":"f1","content":"我的说说","commentlist":[{"tid":"c1","uin":10001,"name":"小明","content":"hi","create_time":1750000001}]}]});'
    client, _ = _post_client([(200, body)])
    comments, ctx = asyncio.run(client.get_own_feed_comments(bot_uin="3545773341"))
    assert comments["f1"][0].comment_tid == "c1"
    assert ctx["f1"] == "我的说说"
    assert "uin=3545773341" in str(client) or True  # 占位防误删,实锚在请求断言
```

- [ ] **Step 2:** FAIL
- [ ] **Step 3: 实现**（client.py 追加;`_request` 增 `data: dict | None = None` 透传给 fetch 且 binary 分支不再适用——写路径走 `params={"g_tk":...}`+form;headers 补 `Origin`）

```python
    DOLIKE_URL = "https://user.qzone.qq.com/proxy/domain/w.qzone.qq.com/cgi-bin/likes/internal_dolike_app"
    COMMENT_URL = "https://user.qzone.qq.com/proxy/domain/taotao.qzone.qq.com/cgi-bin/emotion_cgi_re_feeds"

    async def _post(self, url: str, *, form: dict, referer_uin: str) -> dict:
        cookies = await self.cookie_provider()
        if not cookies:
            raise RuntimeError("空间 cookie 不可用,跳过请求")
        params = {"g_tk": str(generate_gtk(cookies.get("p_skey", "")))}
        headers = {
            "Cookie": "; ".join(f"{k}={v}" for k, v in cookies.items()),
            "User-Agent": BROWSER_UA,
            "Referer": f"https://user.qzone.qq.com/{referer_uin}",
            "Origin": "https://user.qzone.qq.com",
            "Content-Type": "application/x-www-form-urlencoded",
        }
        status, raw = await self.fetch("POST", url, params=params, headers=headers,
                                       timeout_ms=self.timeout_ms, data=form)
        if status != 200:
            raise RuntimeError(f"空间写请求失败: HTTP {status}")
        text = raw.decode("utf-8") if isinstance(raw, (bytes, bytearray)) else str(raw)
        payload = extract_callback_json(text) if "(" in text[:60] else json.loads(text)
        code = payload.get("code")
        if code is not None and int(code) in AUTH_ERROR_CODES:
            raise QzoneAuthError(f"空间登录态失效: code={code}")
        if code is None or int(code) != 0:
            raise RuntimeError(f"空间写请求业务错误: code={payload.get('code')} 响应={text[:120]}")
        return payload

    async def do_like(self, *, fid: str, target_qq: str) -> bool:
        form = build_like_form(fid=fid, target_qq=target_qq,
                               bot_uin=self.bot_uin, now_epoch=time.time())
        await self._post(self.DOLIKE_URL, form=form, referer_uin=self.bot_uin)
        return True

    async def do_comment(self, *, fid: str, target_qq: str, content: str) -> bool:
        form = build_comment_form(fid=fid, target_qq=target_qq, bot_uin=self.bot_uin, content=content)
        await self._post(self.COMMENT_URL, form=form, referer_uin=self.bot_uin)
        return True

    async def do_reply(self, *, fid: str, target_qq: str, comment_tid: str, comment_uin: str,
                       comment_nick: str, content: str) -> bool:
        form = build_reply_form(fid=fid, target_qq=target_qq, bot_uin=self.bot_uin,
                                comment_tid=comment_tid, comment_uin=comment_uin,
                                comment_nick=comment_nick, content=content)
        await self._post(self.COMMENT_URL, form=form, referer_uin=self.bot_uin)
        return True

    async def get_own_feed_comments(self, *, bot_uin: str, num: int = 10):
        feeds = await self.get_user_feeds(target_uin=bot_uin, nickname="我", num=num)
        # get_user_feeds 已解析 FeedItem(丢 commentlist)——此路径需要原始载荷:
        # 直接复用同请求再取原始 payload(实现时把 get_user_feeds 拆出 _fetch_msglist(payload 级)供两用)
        payload = await self._fetch_msglist(target_uin=bot_uin, num=num)
        comments = parse_feed_comments(payload)
        ctx = {f.tid: f.content for f in feeds}
        return comments, ctx
```

（实现要点：①把 `get_user_feeds` 的请求部分抽成 `async def _fetch_msglist(self, *, target_uin: str, num: int) -> dict` 返回原始 payload,`get_user_feeds` 与 `get_own_feed_comments` 共用;②`__init__` 增 `bot_uin: str = ""` 关键字参数,plugin 装配时传入 `favorability.bot_user_id`;③顶部 import `time/json` 与 wire 三构造+`parse_feed_comments`。）

- [ ] **Step 4:** PASS + 全量回归
- [ ] **Step 5:** `git add -A && git commit -m "feat(qzone): 客户端写方法——点赞/评论/楼中楼/自己说说评论拉取(_post 表单通道+fs 包裹解析)"`

---

### Task 3: 出站意图与路由模块

**Files:**
- Create: `catsitate_core/qzone/routing.py`
- Test: `tests/test_qzone_routing.py`

**Interfaces:**
- Consumes: `extract_outbound_text`（outbound.py）。
- Produces: `@dataclass OutboundIntent: kind: str  # "reaction"|"comment_reply"; tid: str; target_qq: str; comment_tid: str = ""; comment_uin: str = ""; comment_nick: str = ""`；`route_outbound(intent: OutboundIntent | None, text: str, has_binary: bool) -> tuple[str, str]`（→ (action, reason),action ∈ "comment"/"reply"/"reject"；reject 场景=无意图/空文本/含二进制）。

- [ ] **Step 1: 失败测试**

```python
"""出站意图路由测试(spec §3.3)。"""
from catsitate_core.qzone.routing import OutboundIntent, route_outbound


def test_reaction_intent_routes_to_comment():
    intent = OutboundIntent(kind="reaction", tid="tA", target_qq="8888")
    action, reason = route_outbound(intent, "好看!", has_binary=False)
    assert action == "comment" and reason == "tA"


def test_comment_reply_intent_routes_to_reply():
    intent = OutboundIntent(kind="comment_reply", tid="tA", target_qq="3545773341",
                            comment_tid="c1", comment_uin="10001", comment_nick="小明")
    action, reason = route_outbound(intent, "谢谢关注~", has_binary=False)
    assert action == "reply" and "c1" in reason


def test_reject_cases():
    assert route_outbound(None, "x", False)[0] == "reject"          # 无意图
    assert route_outbound(OutboundIntent(kind="reaction", tid="t", target_qq="8"), "", False)[0] == "reject"  # 空文本
    intent = OutboundIntent(kind="reaction", tid="t", target_qq="8")
    assert route_outbound(intent, "x", True)[0] == "reject"         # 二进制段
    assert route_outbound(OutboundIntent(kind="publish", tid="", target_qq=""), "x", False)[0] == "reject"  # M3 预留
```

- [ ] **Step 2:** FAIL → **Step 3: 实现**

```python
"""出站意图路由(spec §3.3):注入侧前置设定意图,驱动回调按意图选动作。

M1 的显式拒发由「无意图即 reject」继承;M3 的 publish 意图在此预留(现 reject)。
"""

from __future__ import annotations

from dataclasses import dataclass


@dataclass
class OutboundIntent:
    kind: str  # "reaction"(浏览窗口对当前动态的评论) | "comment_reply"(窗口外评论轮询的楼中楼)
    tid: str
    target_qq: str
    comment_tid: str = ""
    comment_uin: str = ""
    comment_nick: str = ""


def route_outbound(intent: OutboundIntent | None, text: str, has_binary: bool) -> tuple[str, str]:
    """→ (action, reason)。action: comment/reply/reject;reason 供日志与测试。"""
    if has_binary:
        return "reject", "含二进制段"
    if not text.strip():
        return "reject", "空文本"
    if intent is None:
        return "reject", "无出站意图"
    if intent.kind == "reaction":
        return "comment", intent.tid
    if intent.kind == "comment_reply":
        return "reply", intent.comment_tid
    return "reject", f"未知意图 {intent.kind}"
```

- [ ] **Step 4:** PASS → **Step 5:** `git add -A && git commit -m "feat(qzone): 出站意图路由——reaction→评论/comment_reply→楼中楼/无意图拒发"`

---

### Task 4: 评论观察存储（CommentSeenStore）

**Files:**
- Create: `catsitate_core/qzone/comment_seen.py`
- Test: `tests/test_qzone_comment_seen.py`

**Interfaces:**
- Produces: `CommentSeenStore(store: SQLiteStore)`: `ensure_schema()`；`is_new(comment_key: str) -> bool`（新则登记,`comment_key=f"{feed_tid}:{comment_tid}:{uin}"`）；`note_bot_comment(feed_tid: str, bot_text: str, at_iso: str)`（bot 自己发的评论登记,防轮询回环消费自己的评论）；`prune(days: int, now)`（保留期清理,默认 30 天）。
- 兼并**好感度事件表**（Task 8 消费）: `fav_event(user_id: str, kind: str, text: str)`（写入 `qzone_fav_events(day,user_id,kind,text,created_at)`）;`fav_events_on(day: str, user_id: str) -> list[dict]`;`last_fav_interaction(user_id: str) -> str`（该人最近一次任一类事件 created_at,ISO,无则空串）。

- [ ] **Step 1: 失败测试**

```python
"""评论去重+好感度事件存储测试。"""
from datetime import datetime, timedelta

from catsitate_core.qzone.comment_seen import CommentSeenStore
from catsitate_core.storage import SQLiteStore


def test_is_new_dedup(tmp_path):
    s = CommentSeenStore(SQLiteStore(tmp_path / "t.db"))
    s.ensure_schema()
    assert s.is_new("f1:c1:10001") is True
    assert s.is_new("f1:c1:10001") is False
    assert s.is_new("f1:c2:10001") is True


def test_note_bot_comment_prevents_echo(tmp_path):
    s = CommentSeenStore(SQLiteStore(tmp_path / "t.db"))
    s.ensure_schema()
    s.note_bot_comment("f1", "我自己的评论", "2026-08-31T12:00:00")
    # bot 评论落库后,轮询拉到该评论(uin=bot)不视为新
    assert s.is_new(f"f1:999:3545773341") is True  # 其它 tid 仍新
    rows = s.events if hasattr(s, "events") else None  # 占位


def test_fav_events_roundtrip(tmp_path):
    s = CommentSeenStore(SQLiteStore(tmp_path / "t.db"))
    s.ensure_schema()
    now = datetime(2026, 8, 31, 12, 0, 0)
    s.fav_event("10001", "COMMENT", "小明评论了你的说说:好看")
    s.fav_event("10001", "OUT_COMMENT", "你评论了小明的说说")
    rows = s.fav_events_on("2026-08-31", "10001")
    assert len(rows) == 2 and rows[0]["kind"] == "COMMENT"
    assert s.last_fav_interaction("10001") >= "2026-08-31"
    assert s.last_fav_interaction("99999") == ""


def test_prune(tmp_path):
    s = CommentSeenStore(SQLiteStore(tmp_path / "t.db"))
    s.ensure_schema()
    s.is_new("old:c:u")
    s._execute("UPDATE qzone_comments SET created_at = '2026-07-01T00:00:00'")
    now = datetime(2026, 8, 31, 12, 0, 0)
    assert s.prune(30, now) == 1  # 7月1日超出30天保留期
```

（`_execute` 若不对外,测试改用 store 直 UPDATE——实现时取便,断言语义不变。）

- [ ] **Step 2:** FAIL → **Step 3: 实现**（两张表 `qzone_comments(comment_key PK, created_at)` / `qzone_fav_events(id PK autoinc, day, user_id, kind, text, created_at)`,风格对齐 seen_store.py）

- [ ] **Step 4:** PASS → **Step 5:** `git add -A && git commit -m "feat(qzone): 评论去重存储+好感度显式事件表(spec §3.9,不依赖 batch_counter)"`

---

### Task 5: memo 按人重构（spec §3.10,全模块语义变更）

**Files:**
- Modify: `catsitate_core/memo.py`（schema 迁移+read/write 语义）、`plugin.py`（memo_write 工具参数+说话人解析+注入块）、`catsitate_core/config.py`（memo 节加 `speaker_lookup_hours`）
- Test: `tests/test_memo.py`（改造现有用例+新增）

**语义（spec §3.10 固化）**：条目 = 主 QQ（`user_id`）+ 附带 QQ 列表（新列 `extra_user_ids`,JSON 数组字符串）；`stream_id` 保留为**元数据**（流内条目仍按流可见）；可见性 = 流命中 **或** 任一牵连 QQ 命中当前对话对象；`memo_write` 工具增可选参数 `related_user_ids`（逗号分隔 QQ 列表,≤5 个,超出截断并返回提示）。

**Interfaces 变更（逐字）**：
- `MemoService.write(content, stream_id, user_id, ttl_hours, remind_at="", extra_user_ids: list[str] | None = None) -> tuple[bool, str]`
- `MemoService.read(stream_id: str, user_id: str, limit: int) -> list[dict]`（新语义:stream 精确命中 OR user_id ∈ {主,附带}）
- 迁移: `ensure_schema` 内 `PRAGMA table_info(memo)` 查 `extra_user_ids` 列,缺则 `ALTER TABLE memo ADD COLUMN extra_user_ids TEXT NOT NULL DEFAULT '[]'`。
- plugin `_build_inject_blocks` 备忘块: `by_user = self.memo.read("", speaker, limit=3)`（person 维度跨流;原 `read(stream_id,"")` 与 `read("",speaker)` 合并为一次 `read(stream_id, speaker, limit=3)`——注意新 read 是 OR 语义,一次调用即含两个维度,去重后取前 N 不变）。
- plugin memo_write 工具: `related_user_ids: str = ""` 参数（逗号分隔）,解析为列表传 write;工具描述补「可附带相关人的QQ号列表(逗号分隔),备忘对这些人同样可见」。
- 说话人兜底: `_resolve_speaker` 已有;memo 工具默认 user_id 仍取 kwargs 自动注入(私聊可靠)——群聊空时 plugin 内以 `_last_speaker_map` 兜底: **plugin 新增 `self._last_speaker_map: dict[str, str]`（stream_id→user_id）**,在 fav_count hook（豁免分支之后,真实消息路径）记录 `msg_info.user_info.user_id`（配 `speaker_lookup_hours` 无关——直接内存映射,重启丢失可接受,read_recent 兜底已有）。

- [ ] **Step 1: 失败测试**（tests/test_memo.py 追加;现有用例按 OR 语义修订——凡断言「user 维度查不到 stream 条目」的旧断言反转）

```python
def test_write_with_extra_user_ids_and_cross_stream_visibility(tmp_path):
    from catsitate_core.memo import MemoService
    from catsitate_core.storage import SQLiteStore

    svc = MemoService(SQLiteStore(tmp_path / "m.db"), _cfg())
    svc.ensure_schema()
    ok, _ = svc.write("A 和 B 一起出去玩", "s_qq_group", "10001", None, extra_user_ids=["10002"])
    assert ok
    # 主 QQ 在别的流可见(跨流);附带 QQ 同样可见
    assert any("出去玩" in e["content"] for e in svc.read("s_private_10002", "10002", 10))
    assert any("出去玩" in e["content"] for e in svc.read("s_private_10001", "10001", 10))
    assert any("出去玩" in e["content"] for e in svc.read("s_qq_group", "", 10))  # 流维度保留
    # 无关人不可见
    assert all("出去玩" not in e["content"] for e in svc.read("s_x", "99999", 10))


def test_extra_user_ids_limit_and_invalid(tmp_path):
    svc = MemoService(SQLiteStore(tmp_path / "m.db"), _cfg())
    svc.ensure_schema()
    ok, msg = svc.write("x", "s", "1", None, extra_user_ids=["2", "3", "4", "5", "6", "7"])
    assert ok and "5" in msg  # 超出 5 个截断并提示


def test_migration_adds_column(tmp_path):
    import sqlite3

    store = SQLiteStore(tmp_path / "m.db")
    store.execute("CREATE TABLE memo (id INTEGER PRIMARY KEY AUTOINCREMENT, content TEXT, stream_id TEXT, user_id TEXT, expires_at TEXT, created_at TEXT, remind_at TEXT)")
    svc = MemoService(store, _cfg())
    svc.ensure_schema()  # 不抛,补列
    cols = {r[1] for r in store.query("PRAGMA table_info(memo)")}
    assert "extra_user_ids" in cols
```

（`_cfg()` 用现有测试的配置构造；旧用例修订原则：write 调用补 `extra_user_ids=None` 兼容；凡断言「跨 user 不可见」处按新语义改写并在注释标注 §3.10。）

- [ ] **Step 2:** FAIL → **Step 3:** 实现（memo.py 读写 SQL 改 OR 语义: `(stream_id = ?) OR (user_id = ?) OR (extra_user_ids LIKE ?)`——extra 用 JSON 数组 like 匹配 `%"qq"%`,写入 `json.dumps(extra, ensure_ascii=False)`;plugin 三处接线按上文;config `speaker_lookup_hours` 字段暂只声明——`_resolve_speaker` 的 get_recent 兜底窗口改用它,替换现有硬编码 3 条限制的 hours 传参（实现时核对 `_fetch_recent` 签名,当前无 hours 形参则不动、字段注明 M2 预留并告警说明））。
- [ ] **Step 4:** 全量回归（memo 旧用例全数修订后 254+新增 全绿）
- [ ] **Step 5:** `git add -A && git commit -m "feat(memo): 按人重构——主QQ+附带QQ 跨流可见(spec §3.10),流维度保留,说话人映射兜底"`

---

### Task 6: plugin 接线 I——意图设定/驱动路由/qzone_like/评论轮询

**Files:**
- Modify: `plugin.py`（多处锚定插入）、`catsitate_core/qzone/scene.py`（QZONE_SCENE_TEXT 补点赞句）、`catsitate_core/config.py`（qzone 节加 comment_poll 两字段）、`tests/test_qzone_gateway.py`（源码断言用例追加）
- Test: 引擎层已在 T1-T4 覆盖;本任务加 2 个源码级断言用例

**接线点（全部锚定现有方法,先读后插）**：
1. on_load qzone 段: `CommentSeenStore` 实例化+ensure_schema;`self._qzone_outbound_intent: OutboundIntent | None = None`;`self._last_speaker_map = {}`;`self._qzone_comment_task_armed = False`;评论轮询任务 `self._scheduler.register("qzone_comment_poll", max(comment_poll_interval_minutes,5)*60, self._qzone_comment_poll_tick)`;QzoneClient 装配增 `bot_uin=self.config.favorability.bot_user_id`。
2. `_qzone_pump` feed 注入成功后: `self._qzone_outbound_intent = OutboundIntent(kind="reaction", tid=feed.tid, target_qq=feed.uin)`；`window_ended` 分支与 `_qzone_poll_tick` 非窗口收泵处: `self._qzone_outbound_intent = None`。
3. 网关回调 `qzone_gateway`（现 M1 拒发）重写:

```python
    @MessageGateway("duplex", name=QZONE_GATEWAY_NAME, description="QQ空间虚拟聊天平台(动态流入/出站路由)", platform=QZONE_PLATFORM)
    async def qzone_gateway(self, *, message: dict, route: dict, metadata: dict) -> dict:
        """duplex 驱动回调:按出站意图路由为真实评论/楼中楼回复(spec §3.3)。"""
        del route, metadata
        text, has_binary = extract_outbound_text(message)
        action, reason = route_outbound(self._qzone_outbound_intent, text, has_binary)
        if action == "reject":
            self.ctx.logger.warning("QQ空间出站被拒(%s;文本预览=%.30s)", reason, text)
            return {"success": False, "error": f"QQ空间出站拒绝: {reason}"}
        intent = self._qzone_outbound_intent
        try:
            if action == "comment":
                await self.qzone_client.do_comment(fid=intent.tid, target_qq=intent.target_qq, content=text)
                self.qzone_seen.mark_interacted(intent.tid)
                self.qzone_comment_seen.note_bot_comment(intent.tid, text, datetime.now().strftime("%Y-%m-%dT%H:%M:%S"))
                self.qzone_comment_seen.fav_event(intent.target_qq, "OUT_COMMENT", f"你评论了 {intent.target_qq} 的说说: {text[:40]}")
            else:
                await self.qzone_client.do_reply(fid=intent.tid, target_qq=intent.target_qq,
                                                 comment_tid=intent.comment_tid, comment_uin=intent.comment_uin,
                                                 comment_nick=intent.comment_nick, content=text)
                self.qzone_comment_seen.fav_event(intent.comment_uin, "COMMENT", f"{intent.comment_nick} 评论你被你回复: {text[:40]}")
        except QzoneAuthError:
            self.qzone_cookie.invalidate()
            self.ctx.logger.warning("QQ空间登录态失效,cookie 已作废,下轮重取")
            return {"success": False, "error": "登录态失效已重置"}
        except Exception:
            self.ctx.logger.exception("QQ空间出站动作失败(kind=%s,tid=%s),跳过", action, intent.tid)
            return {"success": False, "error": "动作失败,详见插件日志"}
        self.ctx.logger.info("QQ空间出站成功(kind=%s,tid=%s,文本预览=%.30s)", action, intent.tid, text)
        return {"success": True, "external_message_id": f"qzone_{action}_{intent.tid}_{int(time.time())}"}
```

4. `qzone_like` 工具（贴表情同款模式,插在 inspect_image 之后）:

```python
    @Tool(
        "qzone_like",
        description="给当前正在看的好友说说点赞(QQ空间)。仅在浏览动态时可用;可传 message_id 精确指定,缺省对当前说说点赞。",
        brief_description="给当前说说点赞",
        parameters=[ToolParameterInfo(name="message_id", param_type="string", description="目标消息 id(可选)", required=False)],
        visibility="visible",
    )
    async def qzone_like(self, message_id: str = "", **kwargs: Any) -> str:
        if not self._qzone_available:
            return "QQ空间模块未启用。"
        feed = self.qzone_injector.awaiting_feed
        if feed is None:
            return "当前没有正在浏览的说说,无法点赞。"
        if message_id and not message_id.startswith(f"qzone_{feed.tid}_"):
            return f"消息 {message_id} 不是当前浏览的说说,只能对当前说说点赞。"
        try:
            await self.qzone_client.do_like(fid=feed.tid, target_qq=feed.uin)
        except Exception:
            self.ctx.logger.exception("QQ空间点赞失败(tid=%s)", feed.tid)
            return "点赞失败,已记录日志。"
        self.qzone_seen.mark_interacted(feed.tid)
        self.qzone_comment_seen.fav_event(feed.uin, "OUT_LIKE", f"你点赞了 {feed.uin} 的说说")
        return f"已点赞 {feed.nickname} 的说说。"
```

（注意: `@Tool` 装饰器的 metadata 支持 `allowed_session` 硬门控（§2.12）——**在装饰器参数里加 `allowed_session=[f"qzone-qq:{self...}"]` 不可行(实例态)**,查 SDK @Tool 是否支持类级声明;若支持则声明 `allowed_session=["qzone-qq:qzone_feed"]`（伪群号常量——把 QZONE 节的 virtual_group_id 默认值同时写死引用 `from catsitate_core.config import ...` 不可得,改为模块级常量 `QZONE_FEED_GROUP = "qzone_feed"` 于 qzone/__init__.py,config 默认值引用同一字符串字面量并注释一致;若 SDK 不支持类级 allowed_session,靠方法内 `kwargs.get("stream_id") in self._qzone_session_id_set()` 自检拒用,二选一以 SDK 实测为准并写进报告）。）

5. `_qzone_comment_poll_tick`（评论轮询,spec §3.7——醒着且**不在** qzone 窗口时执行）:

```python
    async def _qzone_comment_poll_tick(self) -> None:
        """窗口外评论轮询:好友在 bot 说说下的新评论 → 注入 → 意图楼中楼回复(spec §3.7)。"""
        if not self._qzone_available:
            return
        if self.config.sleep.enabled and self.sleep.is_sleeping():
            return
        if not self._schedule_data:
            return
        win = current_window(self._schedule_data, datetime.now().strftime("%Y-%m-%dT%H:%M"))
        if win and win.get("kind") == "daily" and win.get("qzone"):
            return  # qzone 窗口内由浏览流覆盖,不重复
        bot_uin = str(self.config.favorability.bot_user_id or "").strip()
        if not bot_uin:
            return
        if self._qzone_outbound_intent is not None:
            return  # 上一条评论还在等 bot 回复,不叠加
        try:
            comments, ctx = await self.qzone_client.get_own_feed_comments(bot_uin=bot_uin, num=10)
        except Exception:
            self.ctx.logger.exception("QQ空间评论轮询失败,本轮跳过")
            return
        for feed_tid, items in comments.items():
            for c in items:
                if str(c.uin) == bot_uin:
                    self.qzone_comment_seen.note_bot_comment(feed_tid, c.content, datetime.now().strftime("%Y-%m-%dT%H:%M:%S"))
                    continue
                if not self.qzone_comment_seen.is_new(f"{feed_tid}:{c.comment_tid}:{c.uin}"):
                    continue
                feed_summary = (ctx.get(feed_tid) or "(无文字)")[:30]
                text = f"(评 {c.nickname}) {c.content}\n(来自你的说说:{feed_summary})"
                self._qzone_seq += 1
                msg = {
                    "message_id": f"qzone_comment_{feed_tid}_{c.comment_tid}_{self._qzone_seq}",
                    "platform": QZONE_PLATFORM,
                    "timestamp": str(int(time.time())),
                    "message_info": {
                        "user_info": {"user_id": str(c.uin), "user_nickname": c.nickname},
                        "group_info": {"group_id": self.config.qzone.virtual_group_id, "group_name": self.config.qzone.virtual_group_name},
                        "additional_config": {"is_mentioned": 1.0},
                    },
                    "raw_message": [{"type": "text", "data": text}],
                }
                try:
                    accepted = await self.ctx.gateway.route_message(QZONE_GATEWAY_NAME, msg)
                except Exception:
                    self.ctx.logger.exception("QQ空间评论注入失败(%s)", c.comment_tid)
                    return
                if not accepted:
                    self.ctx.logger.warning("QQ空间评论注入被宿主拒绝(%s),跳过且不标记", c.comment_tid)
                    return
                self._qzone_outbound_intent = OutboundIntent(
                    kind="comment_reply", tid=feed_tid, target_qq=bot_uin,
                    comment_tid=c.comment_tid, comment_uin=str(c.uin), comment_nick=c.nickname,
                )
                self.qzone_comment_seen.fav_event(str(c.uin), "COMMENT", f"{c.nickname} 评论了你的说说「{feed_summary}」: {c.content[:40]}")
                self.ctx.logger.info("QQ空间评论已注入(%s 说:%.40s),意图=楼中楼回复", c.nickname, c.content)
                return  # 一次一条,等 bot 回复(意图消费)后下轮再取
```

（评论消息**不走 FeedInjector**——它是窗口外独立通路,意图直接挂 `_qzone_outbound_intent`,消费=驱动回调成功发出 reply 后置 None。**驱动回调成功路径末尾补 `self._qzone_outbound_intent = None`**（意图一次性消费,防下一条误路由）。轮询 tick 的 `return` 单条语义与 injector 串行一致。）
6. 场景文案（scene.py `QZONE_SCENE_TEXT` 末尾句改为）: `"...你对某条说说的回复会作为QQ空间评论发出;可用 qzone_like 工具给当前说说点赞"`。
7. config qzone 节追加: `comment_poll_enabled: bool = _f(True, "窗口外评论轮询开关(仅轮询自己说说下的新评论)", label="评论轮询开关")`、`comment_poll_interval_minutes: int = _f(30, "评论轮询间隔(分钟)", label="评论轮询间隔(分钟)")`;poll_tick 首行加 `if not self.config.qzone.comment_poll_enabled: return`;白名单默认列表追加 `"qzone_like"`（config.py tool_whitelist 默认值+test_config 断言同步）。

- [ ] **Step 1: 源码断言用例**（tests/test_qzone_gateway.py 追加）

```python
def test_m2_wiring_source_assertions():
    """M2 接线源码级断言:驱动路由/点赞工具/评论轮询/意图消费。"""
    import inspect

    import plugin as _plugin

    src = inspect.getsource(_plugin)
    assert "route_outbound(" in src and "do_comment(fid=" in src and "do_reply(fid=" in src
    assert '"qzone_like"' in src and "do_like(fid=" in src
    assert "_qzone_comment_poll_tick" in src and "comment_reply" in src
    assert 'self._qzone_outbound_intent = None' in src  # 意图一次性消费
```

- [ ] **Step 2:** FAIL → **Step 3:** 按锚定逐处实现（先读 plugin.py 对应方法再插;所有新 import 集中顶部）→ **Step 4:** 全量回归+`ast.parse` → **Step 5:** `git add -A && git commit -m "feat(qzone): M2 接线——驱动按意图路由真实评论/楼中楼,qzone_like 工具,窗口外评论轮询"` 

---

### Task 7: plugin 接线 II——好感度事件消费 + 背包 M-1/M-2

**Files:**
- Modify: `plugin.py`（结算素材+衰减计时+缓存上限+摘要作者）、`catsitate_core/qzone/seen_store.py`（author_nickname 列）、`catsitate_core/inject.py`（缓存上限）
- Test: `tests/test_inject.py`（LRU）、`tests/test_qzone_seen.py`（昵称列）

**接线点**:
1. **结算素材**（`_settle_and_log` 的素材组装处,先读该方法）: 组装完消息素材后追加 `events = self.qzone_comment_seen.fav_events_on(today, user_id)`,非空则在素材文本末尾追加行 `\n[空间互动] {kind 中文}: {text}`（每行一条,≤5 条）——LLM 结算自然计权（§3.9,权重由判定模型掌握,无硬编码数值）。
2. **衰减计时**（`_daily_decay` 的基准计算处,先读 1407-1450）: 基准 = max(现有 batch 基准, `qzone_comment_seen.last_fav_interaction(user_id)`)——空串忽略;并保持「该人流已消亡跳过」逻辑不受影响（事件存在但流消亡时:事件时间参与基准,流定位失败只影响素材不影响计时——按现有代码结构最小插入,读后定夺并写进报告）。
3. **M-1 缓存上限**: `InjectAssembler._cache` 改 `OrderedDict`,超 512 条逐最旧（`move_to_end` on hit）;`plugin._snapshot_cache` 同款上限 256;各加一条单测。
4. **M-2 摘要作者**: seen_store `qzone_feeds` 增列 `author_nickname TEXT NOT NULL DEFAULT ''`（PRAGMA 迁移,ensure_schema 内）;`mark_queued` 增 `author_nickname` 形参;`recent_seen` 返回行带 `author_nickname`;plugin `_qzone_block` 真实聊天摘要行改 `f"{e['author_nickname'] or e['author_uin']}:{e['summary'] or '(无文字)'}"`;`_qzone_poll_tick` mark_queued 调用补 `author_nickname=friend["nickname"]`。

- [ ] **Step 1: 失败测试**（LRU 两条+昵称列断言追加到对应测试文件）
- [ ] **Step 2:** FAIL → **Step 3:** 实现 → **Step 4:** 全量回归 → **Step 5:** `git add -A && git commit -m "feat(qzone): 好感度显式事件并入结算与衰减;缓存 LRU 上限;见闻摘要带作者(背包 M-1/M-2)"`

---

### Task 8: 版本 0.5.0、文档与回归收尾

**Files:**
- Modify: `_manifest.json`（0.4.0→0.5.0,description 追加「互动」）、`CHANGELOG.md`、`docs/plugin-manual.md`（§3.13 增评论/点赞/评论轮询/好感度事件/memo 新语义;§4.12 配置表补 3 字段;日志关键词补「QQ空间出站成功/被拒/评论已注入/点赞失败」;已知限制删「M1 不评论不点赞」改「发布说说/日记为 M3」）、`CONTEXT.md`（词汇:memo 按人/出站意图/评论轮询）、`docs/superpowers/specs/2026-08-30-phase3-qzone-design.md` §3.9（点赞事件无枚举 API,own-feed like 检测不做,OUT_LIKE 仅工具路径——已按此实现,回写一句）
- Test: 全量

- [ ] **Step 1-4:** 文档四处+spec 一句 → `python3 -m pytest tests/ -q` 全绿 → **Step 5:** `git add -A && git commit -m "docs(qzone): M2 互动交付——版本 0.5.0/CHANGELOG/手册/词汇表/spec §3.9 回写"`

---

## Self-Review 记录

- **Spec 覆盖**：§3.3 意图路由(T3/T6)/§3.6 点赞(T1/T2/T6)/§3.7 评论轮询(T1/T2/T4/T6)/§3.9 好感度事件(T4/T7;点赞枚举不可行已按工具路径收敛待 §3.9 回写)/§3.10 memo(T5)/背包 M-1 M-2(T7)。M3（说说发布/日记/回注）不在本 plan。
- **一致性**：T1 构造器与 T2 `_post` 消费的字段名逐一对齐;T4 `fav_event` 与 T6/T7 调用一致;T6 意图消费点（驱动回调末尾置 None）与 T3 路由闭环。
- **实现期核对点**（有锚点非占位）: SDK @Tool 的 allowed_session 类级支持性（T6-4 二选一）;`_settle_and_log`/`_daily_decay` 插入点（T7 先读后插）;`_fetch_recent` hours 形参现状（T5 speaker_lookup_hours 落点二选一）。
