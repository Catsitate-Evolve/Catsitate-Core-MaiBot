# Changelog

<!-- 版本记录规范:
  - 本文件只记录正式版;开发在 dev 分支进行,过程中的迭代不单独立版本条目,
    周期完毕后随下一个正式版一并记录。
  - 正式版之间提供数据迁移脚本(catsitate_core/migrations.py,按 _schema_version 版本表链式执行)。
  - _manifest.json 的 version 与本文件最新正式版保持一致。
-->

## v1.0.4(2026-09-05) 翻页限流回归修复

- 修复 v1.0.3 引入的回归:发现层共享化时误将浏览层 `QzoneRateLimitError` 分支判为死代码删除,但**翻页穿透路径**(积压补全直发)仍会抛出该异常,落进通用回退触发逐好友路径(N+1 请求放大,生产实机复现)。现恢复该分支:翻页撞限流进入 30 分钟共享退避并终止本轮(发现层阶段终止,零登记,下轮重拉),绝不回退 legacy。
- 退避进入逻辑抽为 `_qzone_enter_rate_limit_backoff` 供共享层与翻页路径共用(warn-once 语义一致)。
- 本版无数据格式变更,无需迁移(版本表维持 v1 基线)。
- 测试基线:618 用例全绿(全离线,含翻页限流回归用例)。

## v1.0.3(2026-09-05) 发现层统一入口:共享缓存与限流退避

- QQ空间发现层(统一时间线端点)引入**单飞+共享缓存+限流退避**的统一入口:浏览层(15 分钟节奏)与通知源B(120 秒节奏)原本各自直调同端点同口径请求,合计约 860 次/天,是生产实机持续触发服务端限流(-10001「network busy」)的主因。合并为一次请求源后,源B 经 10 分钟共享缓存命中,端点调用降至约 144 次/天。
- 限流进入 **30 分钟共享退避**:进入时单条告警,期间浏览与源B 零请求(不再逐轮白撞与刷屏),期满自动恢复探测;`QzoneRateLimitError` 统一转换为返回态,`QzoneAuthError` 与其他异常原样透传(浏览层 legacy 回退、源B 跳过的既有处置不变)。
- 带游标的翻页(积压补全)不经共享层穿透直发,续页游标与首页缓存同源;浏览拉取间距语义不变(缓存命中同样占距)。
- 发现层相关配置描述校准(拉取间隔与共享缓存的交互、翻页上限的「至多 1 次真实调用」口径);TTL 与退避时长为内部常量,不暴露配置。
- 本版无数据格式变更,无需迁移(版本表维持 v1 基线)。
- 测试基线:617 用例全绿(全离线)。

## v1.0.2(2026-09-05) 服务端限流处置

- QQ空间读路径服务限流(`code=-10001`,「network busy」)专项处置:新增 `QzoneRateLimitError` 异常分类(发现层与 msglist 两分类点);浏览发现层限流时**跳过本轮、不回退逐好友路径**(限流期间 1→N+1 请求放大等于加重风控),充实层限流终止本轮,逐好友回退途中撞限流同样终止——已入队条目保留,拉取间距+失败占距为天然退避,下轮自愈。
- 通知源A限流时本轮按空处理,**不再连带丢源B/C**(各源失败处置相互隔离);写路径四工具(评论/回复/点赞/发布)遇 -10001 回执明示「QQ空间这会儿有点忙,稍后再试」。
- 充实层与逐好友回退的 2 秒防风控请求间隔移到循环开头:失败跳过路径同样覆盖间距,修复失败后零间隔连击。
- 本版无数据格式变更,无需迁移(版本表维持 v1 基线)。
- 测试基线:613 用例全绿(全离线)。

## v1.0.1(2026-09-05) 拉取节奏与工具适配打磨

- 浏览拉取改「窗口开始即首拉,间隔=两次拉取的间距」:原固定节奏与窗口无关联动,窗口头部最多空转一个间隔、短于间隔的窗口可被整窗跳过;现在进入 read/send 窗口即拉取一轮(60 秒粒度检出),窗口内刷新仍按 `poll_interval_minutes` 间距执行;仅 send 窗口的发布触发同样入窗即发;入窗首拉不受每日发言上限牵连。
- `update_schedule` 工具适配 QQ空间窗口字段:add 新增 `read_qzone`/`send_qzone` 布尔参数(仅 True 时写入键,与日程生成器输出形态一致;view 早有「(刷空间)/(发说说)」标注,move 自动保留标记);工具入参增加形态矫正(布尔字符串按真值词判定、窗口序号字符串转整数)——宿主不按参数声明类型矫型,裸 `bool()` 会把字符串 "false" 误判为 True。
- 注释与文档内部代号清理:审查/缺陷/裁定/里程碑编号改自包含表述(逐文件 AST 行为零变更验证);日志与配置描述中的残留代号一并清除。
- 本版无数据格式变更,无需迁移(版本表维持 v1 基线)。
- 测试基线:608 用例全绿(全离线)。

## v1.0.0(2026-09-05) QQ空间人格化上线:内容护栏与工程收官

自 v0.3.2 以来的全部开发(2026-08-30 ~ 09-04:QQ空间三期、工具驱动化、表达层、三轮全量复审、内容护栏、数据迁移框架)汇总为首个正式版。升级自 v0.3.2:数据格式由 `ensure_schema` 自愈并打上 v1 基线版本标记,无需手工迁移。

**QQ空间:感知**

- QQ空间动态映射为 `qzone-qq` 虚拟群聊流(连字符别名与真实 QQ 统一 person),串行注入复用主程序 planner→replyer 链;消息时间戳=阅读时刻,发布时间以相对时间前缀写入正文;图片组件对齐 napcat-adapter,VLM 描述链实证打通。
- 日程窗口 `read_qzone`/`send_qzone` 独立标记(可同窗);虚拟流专属场景提示词替换、工具白名单、模块豁免(好感度计数/晚安判定等)。
- 生产注意:NapCat 需可响应 `adapter.napcat.account.get_cookies`;主程序 `experimental.focus_mode` 必须关闭(否则模块自检停用);person 折叠自检失败硬停用(不降级);虚拟流学习落在自身 session,勿配置 `*:*` 全局表达共享组。

**QQ空间:互动与通知**

- 互动三工具:`qzone_comment`(评论,支持 @)/`qzone_reply`(真实楼中楼:commentId+commentUin 二元组精确匹配,防 @ 错人)/`qzone_like`(说说ID锚解析);目标三级解析=注入上下文登记表(内存 LRU)→ 已见表 → 在途项,失败显式拒绝不猜测。
- 好感度显式事件:好友评论/点赞 bot 与 bot 的出站互动双向记入事件表,参与日终结算(LLM 计权)与衰减计时;不依赖消息计数;同日同事件去重;日终候选=当日活跃 ∪ 近 window_hours 回看窗内的空间互动(纯空间好友也结算)。
- 统一通知通道(默认 120 秒,三源):源A=自己说说下的新评论(扫最近 20 条,翻旧账也能发现;含 bot 评论下的楼中楼回复);源B=自己在他人说说下评论收到的楼中楼回复;源C=好友赞了 bot 的说说(「与我相关」流解析,取消赞再赞不重复)。双优先级注入(P1 通知插队于 P2 浏览之前),阅读新→旧;通知是推送语义,不依赖浏览窗口;注入被拒软回退重试上限 3 次,扫描异常回退本轮已登记键(通知不因单次失败静默丢失);通知不强制触发回复(走主程序自然概率,拟人留白)。
- 登录态同轮自愈:AuthError 时作废并强制重取 cookie(NapCat 在线会话)后原地重试一次,覆盖全部空间调用点;重取失败才显式失败回执。
- 业务错误回执带限制语义:-10049(操作频繁)明说「先歇一歇别重试」;不做硬频控,限制写进工具返回。

**QQ空间:统一时间线与协议校准**

- 浏览流「发现层+充实层」两层混合:`feeds3_html_more` 1 次调用覆盖全好友统一时间线,仅对新动态按好友拉取充实——API 量 O(N好友)→O(1+新动态);发现层失败回退逐好友旧路径(告警)。
- 发现层协议经实机探针+开源调研双路实证校准:scope=2 才是全好友动态流(7 天窗口);翻页用 begintime 游标,四重终止;长时间离线后的动态积压逐页补全;窗口边界条目缺 appid 回退解析 appiconid。
- 查看工具:`view_friend_feeds`(全域,翻页,可读格式)/`view_friend_feed_detail`(单条说说全文+全部评论与楼中楼,查看即已见并登记评论级锚 comment_map);`inspect_image` 支持 image_hash 前缀直查。
- 浏览注入带评论区(数据取自充实层现有载荷,零新增 API);说说图片出口公共管线:多图(≥2)拼接为一张 3 列网格角标合成图(VLM/上下文成本 O(N)→O(1)),单图直发;图片压缩移出事件循环;图片锚 hash=拟合后实际送出字节。

**QQ空间:表达**

- `qzone_post` 说说发布工具+表达润色层:「说什么」归模型(草稿直写),「怎么说」归润色层(按人设与表达方式顺稿;完全重组许可但不修改关键事实);润色失败不阻断,告警后以草稿直发;超长只做一次软性重润,不硬截断。
- 发布主动触发:`send_qzone` 窗口经 proactive.trigger 注入分享指示(同窗形态刷完再发、仅发形态窗口即触发);冷启动种子自举建会话;发布保留新说说 tid 三处登记(自己发的内容自己认得)。
- 睡前日记:入睡时以 bot 本人身份、按当日真实素材(日程/备忘/空间见闻/全天聊天记录/真实天气)生成第一人称日记(篇幅区间由配置指导,生成温度可配置),经发布 API 深夜直发,醒来回注虚拟流;模板明令不得编造素材外内容。
- 空间见闻:read_qzone 窗口结束时旁路 LLM 把近 24h 滚动窗内的浏览与互动摘要为 60~150 字「空间见闻」,真实聊天注入优先输出(跨零点会话素材自然衔接;截断统一保留最新;无当日见闻回退「近期刷到」列表)。

**QQ空间:工具驱动架构**

- 虚拟流网关 receive(只进不出):虚拟流里直接打字发不出去,是否互动、评论什么,完全由模型经工具自主决定;场景 prompt 说明 ID 锚格式与工具用法,明示沉默自由(不想互动可以只浏览)。
- 消息格式带 ID 锚(纯图说说也保留文本段承载参数),参数走〔〕独立尾行;`@{uin,nick}` 机器格式解析为「@昵称」可读形态;三动作工具全域可用(白名单只管非 `qzone_*` 工具在虚拟流的可见性)。

**内容护栏**

- `guard` 配置节(`enabled`+`patterns` 全局正则单列表)与纯匹配器;三拦截点=空间动作工具(润色后、发布前取消)/日记(发布前取消)/replyer(`after_response` EARLY 钩子改空 response,真静默);非法正则整组拒绝加载;配置热重载即时重建。

**数据迁移框架**

- `_schema_version` 版本表(全 SQL 参数绑定)+迁移步进注册表+链式执行;handler 幂等;v1.0.0 打基线标记;后续正式版间的数据变更走此通道。

**修复(v0.3.2 既有功能)**

- reply 上下文补传形状修正:主程序实际下发 `FunctionCallItem`+`tool_call` 形态,旧匹配自一期起从未生效——补传首次真正工作。
- 旁路 RPC 超时显式告警:RPCError 的 E_TIMEOUT 以「RPC 超时(…30000ms)」形态浮出(原先只记异常类型名),覆盖日程/衰减/结算/图片重看等全部旁路调用点位。
- 好感度自然衰减并发防护:醒来补衰减与调度 tick 并发时不再双计。

**仓库与文档工程**

- 删除废弃配置字段(poke.enabled 死开关等)与死代码;旁路模板 8→12 个(新增场景/日记/表达/见闻),内置兜底与 `prompt_templates/` 权威源逐字一致(镜像测试锁定);开发文档库 `docs/dev/`(设计思想/架构/模块/测试体系/里程碑)为仓库唯一文档源,随代码同步维护;版本记录只含正式版,开发在 dev 分支进行。
- 测试基线:602 用例全绿(全离线,无需主程序与 QQ 登录态)。

## v0.3.2(2026-08-18,旁路模板自动部署)

- **旁路模板自动部署**:插件加载时(`on_load`)自动把 `prompt_templates/catsitate_*.prompt`(8 个)同步到主程序 `prompts/zh-CN/`(内容一致跳过、变更覆盖;主程序 `load_prompts()` 在插件启动后调用,同次启动即生效,无需手动复制/重启)。WebUI「提示词管理」即可查看/编辑这 8 个模板(编辑产物写 `data/custom_prompts/zh-CN/`,插件优先读取)。插件不在 `plugins/` 下或主程序 `prompts/zh-CN/` 缺失时显式告警跳过,不阻断加载,插件回退内置默认
- 含单元测试(首次部署/幂等跳过/变更覆盖/目录缺失跳过/无关文件不动/写入失败显式告警)

## v0.3.1(2026-08-18,公测修复集)

- **睡眠窗口语义对齐**(联调裁定):睡眠窗口 = 可入睡时间——静默关闭 = 窗口起点直接入睡;静默开启 = 窗口起点后安静满 `silent_sleep_minutes` 分钟入睡(计时基准 = max(窗口起点, 最后活动时刻));晚安判定仅在睡眠窗口内有效(与静默开关无关);窗口终点仍未入睡 → 不入睡,但补执行入睡时的任务(次日日程生成,每窗口一次)
- **跨午夜睡眠窗口保留**:过期日程(早于今天)若其睡眠窗口仍覆盖当前时刻则保留恢复;换日 tick 时旧日程睡眠窗口仍在进行则保留旧日程直至窗口结束(公测发现:直接删除/替换会导致当天无法入睡)
- **配置修复**:7 个 `*_timeout_ms` 字段默认值 `None`→`0`(0=主程序默认超时),修复主机 tomlkit 无法序列化 None 导致的配置回写崩溃、插件激活失败(含回归测试)
- **RPC 帧超限修复**:取消息不再携带二进制数据(大附件消息曾撑爆 16MB RPC 帧),结算/衰减取数按流隔离(单流失败只跳过该流)
- 命令修复:命令别名多余斜杠;新增 GPL-3.0 许可证与 config_back 忽略规则

## v0.3.0(2026-08-16,按人重构)

- 好感度按人唯一标识(user_id):favorability 单行按人存储与判定(跨流聚合),日终结算按人合并一次,衰减计时按人跨流取最近互动;`batch_counter` 保留 (user, stream) 行仅作活跃度记录
- 「特别」等级独占:全表任意时刻最多 1 人「特别」(≥100 分),他人升入被占位时钳制 99 分(挚友)并显式日志;独占者掉出后空位释放
- 主动问候统一(原 2.3 与 greeting 合并):仅「特别」等级 + 私聊流存在,greeting 窗口起点触发,无每日一次限制;删除 `greet_threshold_level`/`private_threshold_level` 配置
- 配置字段清理:删 `speak_threshold_level` 以外旧门槛配置;`batch_counter` 移除 `window_start` 死列(旧形状检测自动重建)
- 公测前最终审查修复:好感度分数负分钳制(最低 0,规格 §3.1);次日日程夜间重启恢复(date ∈ 今天/明天,过期才删);LLM 异常日志只记异常类型(防请求体/PII 入日志);日终结算逐用户 try/except 隔离;debug 日志卸载清理(on_unload 移除并 close handler、恢复 logger 级别);manifest 能力对账(删除零调用能力)与版本号 0.3.0;衰减天数判定改浮点(「距今 > N 天」语义);early 幂等键加用户后缀;remind_fired 兜底 tick 同步日键清理;fav_count 同查睡眠模块开关;catsitate.db 与睡醒回顾报告 0600 权限;旁路告警阈值 `==` 改 `>=`

## v0.2.0(2026-08-15,二期)

- 好感度自然衰减:LLM 判定拟人化衰减(未互动 N 天,0~-decay_max),群聊 quote/@ 防误判
- 睡眠管理:睡眠=日程窗口(LLM 自主作息)、绝对静默拦截、晚安判定入睡、睡醒回顾报告
- 日程:入睡时生成次日动态活动日程(1 睡眠+1~8 活动)、日程块注入、日程窗口 trigger 主动发言(表达权交主程序)、update_schedule 工具
- 主动私聊:挚友级私聊问候(2.3)
- 备忘录提醒时间 remind_at:日程收录 + 无日程独立兜底注入
- 旁路模板:二期新增 4 个(decay/sleep_confirm/schedule_generate/sleep_review),随一期机制接入主程序「提示词管理」页统一编辑

## v0.1.0(2026-08-15,一期联调完成)

主要功能:
- 注入框架:环境/备忘/好感度块前插 system 之后(缓存友好分层注入);等级规则按等级单条注入好感度块最前(联调决定,省 token 且提升缓存命中)
- 好感度:批次结算制(提前结算 + 日终兜底顺延)、LLM 判定结合 bot 人设背景、delta 上限可配置(delta_max)、5 级规则独立配置字段、bot 发言识别(bot_user_id)
- 备忘录:双通道(工具+命令)、单条 TTL、注入当前流+说话人两维度
- 贴表情:内置 30 项精选 QQ 表情表(替代可配置白名单,联调决定)+ 每流冷却,仅群聊可用
- 戳一戳:主动戳工具(仅冷却限制);入站通知解析已按联调结论删除
- reply 上下文补传(规则层)+ LLM 哨兵层(可选,默认关)
- 图片重看工具(VLM)
- 时间感知:节日/节气/天气环境块(默认珠海;lunar-python 缺失时公历回退链不受影响)
- 旁路 LLM:经主程序 model_task_config 的 task 名路由(各能力独立配置 + 独立超时);4 个能力 prompt 模板接入主程序「提示词管理」页统一编辑(custom_prompts 覆盖优先,模板变更即缓存失效)

细节:
- 旁路 LLM 调用记账(llm_usage)+ 每日告警阈值
- 60s 后台调度器(天气/节日/备忘清理/日终结算),周期随配置热重载
- 92+ 单元测试;实机联调验收通过
