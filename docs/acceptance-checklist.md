# 手动验收清单(规格 §5)

前置:插件已放入 plugins/ 并启用;WebUI 插件页打开 `plugin.enabled = true`;重启 MaiBot 后日志出现 `catsitate_core 已加载`。

- [x] 命令:`/记一下 周四交作业` → bot 回复已记下;`/记一下` 超 80 字符 → 提示精简
- [x] 工具(planner 自主):「帮我记一下…」→ memo_write;「戳一下他」→ poke_user;「看看刚才那张图里写了什么」→ inspect_image;均已实机确认
- [x] 贴表情(仅群聊):群聊中「给上一条消息贴个表情」→ msg_react;私聊调用 → 返回「贴表情仅限群聊」
- [x] 注入:日志或调试输出中 `[等级规则]`/`[环境]`/`[备忘]`/`[好感度]` 片段出现,且位于 system 之后、历史之前
- [x] 好感度:early 结算与 daily 结算均实机验证(delta/note 落库,顺延分支亦验证);跨天常规运行待观察
- [x] 好感度材料中 bot 发言识别:实机验证结算取数 86 条中 37 条 bot 发言正确标注(bot_user_id=3545773341)
- [x] 主动戳(戳一戳):「戳一下他」→ poke_user 已可用;入站戳一戳解析已按联调结论删除
- [~] 贴表情防刷:同流 30 秒冷却(低优先级,联调决定不单独测试,单测覆盖)
- [x] 哨兵层:实机验证「哨兵判定:放行回复」+ llm_usage sentinel 记账;键名/人设读取已按主程序 payload 修复
- [x] 旁路记账:`data/plugins/catsitate.core/catsitate.db` 中 `llm_usage` 表按模块分列调用数(实测 favorability/msg_react/image_relook 分列)
- [x] 热重载:WebUI 修改配置后 `on_config_update` 生效(`配置已刷新,派生缓存已重置` 日志)

## 二期(2026-08-15)

- [x] 衰减:全链路验证(扫描→LLM 判定含人设/注记/天数→delta 钳制→落库→「好感度衰减 …delta=0」日志→计时基准重置);正式配置已恢复(7 天阈值/24h 调度)
- [x] 睡眠:兜底强制入睡+已入睡日志(12:39/12:22 两次);睡眠中消息被 Hook 中止、无 planner、无回复;缓冲持久化(修复 JsonSnapshot dict-only)
- [x] 睡醒回顾:报告文件生成含 LLM 摘要(「这不重要,bot 醒来无需知道」);自然醒+醒来补跑结算验证
- [x] 日程:入睡确认生成次日(LLM 自主排布作息);日程块注入含执行状态与接下来
- [x] 主动发言:到达活动窗口且存在满足门槛的活跃流 → 日志「主动触发[date] -> stream:活动」(16:24 深夜活动触发 + 消息送达)
- [x] 2.3:挚友级私聊用户,greeting 窗口收到主动问候(16:55「主动私聊触发[day] -> user」+ 消息送达;manifest 补 maisaka.proactive.trigger 能力、SDK 解包形状修复)

## 二期·按人重构(2026-08-16)

- [x] 建表按人:重启后 favorability 单行 per user(列 user_id/level/score/note/window_start/judged_at),启动零错误
- [x] 特别独占(单元测试守护):A 上位→B 钳 99/挚友(clamped_exclusive)→A 加分不受限→A 掉出释放→B 补位(实机需双真人,跳过)
- [x] 主动问候统一:负例(特别者 999999999 无私聊流 → 18:36:38 跳过日志);正例(特别者 3341299096 → 18:42:10「主动问候触发」+ 消息送达);无每日一次限制(18:44:51 同窗口再次问候)
- [x] 按人结算:18:44:54「好感度结算 3341299096:early delta=0」(early 阈值临时 20→3 验证,已恢复)
- [x] 2.1 回归:18:52:51「主动触发 -> 4d764c...:读书」(按人等级判定)
- [x] 静默入睡(默认开启):临时阈值 1 分钟 + 洗漱窗口 20:12 → 20:15:02「静默入睡:安静 1 分钟」→ 入睡;醒来时间按 clamp 封顶 660 分钟(20:15+660=次日 07:15);验证后配置/日程/睡眠状态已恢复
- [x] reply 补传回归(上下文工具列表内置常量):实机 memo_read 调用后按设计跳过(thinking 模型恒有推理,三条件不齐),无异常;行为由单测覆盖
- [x] 完整链路联调(20:22-20:44):晚安判定入睡(20:29:45)→ 次日日程生成(20:30:43,无 WARN,7 窗口全分钟精度,秒格式修复实机验证)→ 睡眠拦截 3 条消息进缓冲且绝对静默 → 自然醒(20:35:25)+ 回顾报告(聚合+摘要正确识别重要消息)+ 醒来补跑结算(delta=1)→ 备忘 remind_at=20:41 写入 → 20:42:25 兜底注入 → 对话兑现提醒
- [ ] 衰减按人:跨流取最近互动实机验证(需 7 天未互动场景,单测覆盖,实机待观察)
- [x] update_schedule:view+增删改完整链路(bot 成功把下午改为「一起听歌」);日程块注入(修复 BLOCK_ORDER 后)与重启恢复(schedule.json)
- [x] 备忘提醒:remind_at 传参(描述强化后 LLM 正确传 10:04)、到点兜底注入(10:08 日志)、去重持久化、bot 对话中兑现提醒
